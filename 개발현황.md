# 개발 현황 (제작팀)

> 최종 갱신: 2026-08-13 · 현재 버전: 전투 프로토타입 **v0.40** (카드 픽셀 성도 · 누끼 판화 · 말풍선 직렬화 · 조력자 이펙트)

## 현재 버전

`starry-night-v0.40.html` (저장소) = `전투프로토타입_v0.40.html` (로컬 한글명 — 원본 PC에서 복사로 생성할 것) — 시작 화면(튜토리얼 / **1막 → 2막 → 3막 여정** / 보스전 단판 / 도감 138종). 모바일 터치 지원. **약 1.45MB**. 버전별 변경 내역은 아래에 역순이 아니라 **작성순**으로 쌓여 있다 — v0.21부터는 문서 하단에 있다.

## v0.20 변경 (사용자 피드백 12건 + 대장간 제안)

### ① 대장간 — 같은 별자리 2장을 하나로 (사용자 제안)
맵 **대장간 노드 3개** + **휴식의 '합성' 선택지**. 재료 A를 고르면 **같은 별자리 카드만 활성화**되고, 결과 카드를 미리 본 뒤 확정한다.
**비용 = 둘 중 높은 쪽 · 효과 둘 다 발동 · 성좌 2성 계산 · 별 두 개 동시 점등 · 재합성 불가**(강화된 카드를 재료로 쓰는 건 가능).
> 이 한 수가 3성을 되살렸다 — 궁수 4장 기준 완성률이 전투당 0.26회 → **1.00회**로.

### ② 3성 대성좌 9개 부활 + 카드 195종
황도 7개(쌍둥이·황소·사자·처녀·전갈·궁수·게)를 3성으로 되돌리고 오리온·큰곰 유지. 3성 보너스는 **"그만큼 세게"**(황소 최고HP 18+취약 2턴, 처녀 물질 2+HP 9+1장 등).
3성이 현실적이도록 **황도에 카드 45장 추가**(총 195종) — 별선 데이터도 실제 J2000 좌표로 확장(황소 14성·쌍둥이 15·사자 14·처녀 13·궁수 13·게 9·오리온 11, 기존 `si` 순서 보존).
약한 2성 보너스 14종도 상향(까마귀·작은곰·목동·왕관·케페우스·거문고·큰개·독수리·카시오페이아·고래·용·페가수스·백조·페르세우스).

### ③ 보스 재설계 — 수치 상향 + 기믹 6종 (사용자 "보스가 너무 쉽다")
| 적 | 수치 | 기믹 |
|---|---|---|
| 플레이아데스 | 100/9 | **분열** — HP 50%에서 아기별 2 + 성막 12 |
| 히아데스 | 135/11 | **흐린 하늘** — 4턴마다 별자리 하나를 다음 턴 완성 불가 |
| M42 (1막 보스) | 125/11, 격노 상한 9 | **성운의 장막**(아머 턴에 손패 1장 봉인) · **2페이즈**(HP 50%) |
| M1 게 성운 | 160/13 | **펄서 파동** — 4턴마다 내 성막을 통째로 흡수해 자기 아머로 |
| M13 구상성단 | 150/11 | **밀집** — 아기별이 살아 있는 동안 받는 피해 −20% |
| 블랙홀 (2막 보스) | 210/16 | **사건의 지평선**(손패 2장을 덱 바닥으로) · **계절 역행** · **2페이즈**(HP 40%) |

### ④ 계절이 적에게도 · 관문마다 계절이 넘어간다
딥스카이 6종에 **보이는 계절**을 부여(M42·플레이아데스·히아데스·M1=겨울 / M13·블랙홀=여름) — 제철이면 그 적의 공격력 **+25%**, 역철이면 **−25%**(의도 옆에 표기). 2막은 관문을 밟을 때마다 시작 계절이 **봄→여름→가을→겨울**로 넘어가고 맵에서 예고한다. 1막은 계절 없음 유지.

### ⑤ 실사 에셋 (사용자 제공 파일)
- **조력자 초상 7인**: `조력자.png`에서 흰 배경 제거 + **한글 라벨 자동 제외**(셀에서 가장 큰 연결 성분만 남김) + 원형 페더 → WebP(알파). 조력자 박스·증강 선택 화면·대사 칩에 사용
- **계절 배경 4장**: `배경.png`(2×2)에서 흰 구분선·금테 안쪽으로 크롭 → **전투 화면 배경**으로 cover 합성 + 어둠막(평균 밝기 53). 계절이 바뀌면 크로스페이드. **1막은 이미지 없음**(멈춘 하늘)
- 신규 스크립트 `embed_assets.py` 한 번 실행으로 전 과정 (그림 교체 시 재실행)

### ⑥ UI (사용자 지시 4건)
- **카드 용어 패널**: 손패에 손을 대면 **오른쪽에** 그 카드가 쓰는 용어 풀이가 펼쳐진다(화면 끝 카드는 왼쪽으로 반전). 성좌 조건·완성 보너스도 함께
- **강화 미리보기**: 클릭 즉시 확정이던 것을 **현재 카드 ↔ 강화 후 카드 나란히 + 바뀐 수치 초록 태그 + [강화한다]/[다른 별 고르기]**로. 대장간 합성 미리보기와 같은 컴포넌트
- **조력자 훈수**: 7인 성격별 대사 10상황(전투 시작·보스·성좌 완성·큰 피격·위기·처치·승리) + **2인이면 서로 받아친다**(브라헤↔케플러, 갈릴레오↔코페르니쿠스 등 짝 전용 티키타카). 박스 70 → **126px**, 초상 86px 실사, 왼쪽 말풍선
- **몹 확대**: 일반 180→244px(아트 108→176), 보스·엘리트 **296px**(아트 226), 쫄몹 120→156. 3마리 이상이면 자동 축소

### v0.20 검증 (런타임 에러 0)
- 데이터: 별 195 · 별자리 26 · 필요수 1/16/9 · 초상 7 · 배경 4 · **카드 195종 기본·강화·합성 전수 렌더** · si 전수 유효 · 로어 누락 0
- 대장간: 같은 별자리만 재료 활성 · 비용 max · 효과 2개 · **성좌 2성** · 별 2개 점등 · 재합성 차단 · 실전에서 관통 19(눈키6+루크바트13)로 적 성막 40 무시
- 보스 기믹 주기: 블랙홀 `atk atk def drain swallow def atk drain` · M1 pulsar 4턴 · 히아데스 conseal 4턴 · 펄서가 성막 17을 그대로 흡수 · M13 밀집 20→16 · 플레이아데스 분열(아기별 2 + 아머 12) · M42 손패 봉인(사용 차단 + 봉인 표시) · 블랙홀 2페이즈 상시 중력
- 계절: 2막 관문마다 봄→여름→가을→겨울 · 블랙홀 등장 시 역행 · 여름 블랙홀 공격 14→18(제철) · 배경 평균 밝기 53(가독성 확보) · 1막 이미지 없음
- UI: 용어 패널 4/4 표시(2장 좌측 반전) · 강화 diff("피해 4 → 5") · 합성 미리보기 3장 · 조력자 2인 126px·말풍선 · 회귀(튜토리얼·단판 180·도감 195·상점/휴식/이벤트/덱 보기·1막→보스→유물→조력자→2막→최종 클리어)

## v0.19 변경 (사용자 지시 10건 + 밸런스 시뮬레이션)

### ① 카드 수급 = **별자리 3택1 → 그 별자리 카드 2장** (사용자 지시)
전투 보상이 2단계가 됐다. **별자리 카드**(각인 그림 + 아이덴티티 + 완성 보너스 + 내 성도의 보유 수)를 3장 중 하나 고르면, 그 별자리의 별 전부가 펼쳐지고 **2장을 담는다**. 3택 중 하나는 항상 **이미 모으던 별자리**라 성좌를 이어 완성할 수 있다. 상점도 3장 중 2장을 보유 별자리에서 뽑는다.
> 시뮬레이션: 1막 클리어 **5.3% → 20.2%**, 2막(도달자 중) **25% → 45.5%** (밸런스로그 13차)

### ② 성좌 완성 조건 재조정 (시뮬 근거)
**3성 → 2성 10개**(황소·사자·처녀·전갈·페가수스·안드로메다·백조·궁수·페르세우스·용). 3성 유지는 카드 8장인 **오리온·큰곰**뿐. 근거: 그 별자리 카드를 8장 몰아줘도 3성은 전투당 0.8회, 2성은 4장만 모여도 2.0회. 필요 수를 낮춘 10개는 보너스도 소폭 하향.

### ③ 1막에서 계절 제거 · 2막 계절 배경 연출 (사용자 지시)
- **1막 = 계절 없음** (`R.noSeason`): 고정 계절도 폐지 — 모든 별이 보정 없이 제 값으로 빛난다. 맵/전투 HUD "계절 없음 — 멈춘 하늘"
- **2막 계절 배경**: 계절마다 하늘 색조(봄 연둣빛·여름 푸른빛·가을 주홍·겨울 청백)가 바뀌고 **떠다니는 것**이 달라진다 — 꽃잎 / 반딧불 / 낙엽 / 눈. 계절이 넘어가는 순간 화면 섬광 + 상단 계절 표시가 번쩍인다

### ④ 조력자 7인 · 2막에서 한 명 더 (사용자 지시)
기존 3인 + **요하네스 케플러**(3턴마다 물질 +2) · **니콜라우스 코페르니쿠스**(매 턴 손패 1장 교체) · **에드윈 허블**(전투 시작 +2장) · **헨리에타 리비트**(이번 턴 첫 성좌 완성에 물질 +1). 초상·아이콘 수제 SVG.
**1막 보스 격파 → 2막 조력자 3택1**(증강 스타일) — 2막은 두 사람과 함께 오른다.

### ⑤ 보스 처치 유물 6종 (사용자 지시)
보스를 잡으면 **판을 바꾸는 유물 3택1**: 혼천의(매 턴 물질 +1) · 북극성의 파편(모든 성좌 필요 수 −1) · 영원한 성막(성막이 사라지지 않는다) · 초신성의 심장(전투 시작 전체 18) · 은하수 렌즈(매 턴 +1장) · 황도의 인장(매 턴 첫 카드 −1). 상점·중간보스 드랍 풀에서는 제외.

### ⑥ 아이템 확장 (사용자 지시)
소모품 7 → **13종**(별의 눈물 HP25 · 암흑물질 결정 성막18 · 초신성 뇌관 단일25 · 항성풍 나팔 전체 취약 · 궤도 계산기 이번 턴 필요 수 −1 · 혜성의 얼음 전체 약화 2턴), 소지 한도 **3 → 5**.

### ⑦ 카드 확대 + 손맛 (사용자 지시)
카드 156×272 → **178×308**(메달·비용·아트창·이름 함께 확대), 손패 슬롯 132×222 → 152×252. 전환 곡선을 `cubic-bezier(.16,.84,.24,1)` .34s로 늘려 **미끄러지듯** 움직이고, 호버 확대는 1.42 → 1.36으로 낮춰 과장을 줄였다.

### ⑧ 카드 텍스트 개편 (사용자 지시)
문장형 → **기호 + 짧은 말**. 키워드 SVG 14종(피해·관통·성막·뽑기·물질·회복·중독·화상·약화·취약·가시·봉인·버리기·전체)을 붙이고 설명은 툴팁으로 내렸다. 특수 메커니즘에는 **이름 인장**을 달았다 — 전차·처형·수확·대관·변광·램프·파워·봉인·교대·선택·복제·길잡이·버리기·예열·가속·전조·쌍성·겸용. 성좌 조건도 `✦ 한 턴 2성 → …`로 압축.

### ⑨ 보스·2막 수치 (시뮬 근거)
1막 보스 M42 아머 12/3턴 → **10/4턴** · **격노 상한 +6** 신설 · act1 HP 100 → **92** (보스 승률 40% → 51%). 2막 적 전원 **0.85배**(블랙홀 145/13, M1 115/10, M13 106/9 등) — 2막 도달자 승률 30% → 45.5%.

### ⑩ 밸런스 시뮬레이터 v2 (신규 도구 3종)
`dump_sim_data.js`(빌드 → JSON) · `sim_battle_v2.py`(현행 규칙 재현 + 그리디 AI) · `sim_tune.py`(레버 A/B). 3~12차의 시뮬 공백 해소. 자세한 결과·모델 수정 이력은 밸런스로그 13차.

### v0.19 검증 (로컬 서버 + 브라우저 스크립트, 런타임 에러 0)
- 데이터: 별 150 · 별자리 26 · 아이템 13 · 유물 12(보스 6) · 조력자 7 · 키워드 아이콘 14 · 필요 수 분포 1/23/2 · 아트 누락 0 · **카드 150종 기본·강화판 전수 렌더**
- 신규 아이템 6종 실동작(HP 25 회복 · 성막 18 · 단일 25 · 전체 취약 · 필요 수 −1 · 전체 약화 2턴), 소지 한도 5
- 보스 유물 5종 동시 적용 턴 검산: 물질 4(3+혼천의) · 필요 수 −1 · 손패 6(5+은하수 렌즈) · 비용 −1 · 성막 유지
- 조력자: 허블 첫 턴 7장 · 케플러 3턴째 물질 +2 · 코페르니쿠스 교체 후 손패 5 유지 · 전투 박스 2인 표시
- 보상 흐름: 별자리 패널 3 → 카드 6장 중 2장 → 덱 +2 · 보스 유물 3택1 → 2막 조력자 3택1 → 2막 맵
- 계절: 1막 `noSeason=true`(보정 0, 라벨 "계절 없음") / 2막 턴마다 봄→여름→가을→겨울 + 배경 입자 26→30→24 교체 + 색조 전환
- 카드: 178×308, 인장 "전차", 효과문 `성막 8 · 성막 절반만큼 피해`, 성좌줄 `✦ 한 턴 2성 → …`
- 회귀: 튜토리얼(4단계 문구 갱신) · 단판 보스전(M42 140) · 도감 150장/26그룹 · 상점/휴식/이벤트/덱 보기 · **1막→2막→최종 클리어 전 구간 통과**

## v0.18 변경 (사용자 지시 3건)

### ① 성좌 각인 그림 **누끼(배경 제거)** 재임베드
- 사용자가 배경 제거용으로 다시 저장한 `성좌각인.png`(1536×1024 PNG)에서 배경을 **프로그램으로 제거**하고 17장으로 잘라 개별 WebP(알파)로 임베드. `embed_conart.py` v2가 전 과정을 수행 — 그림을 갈아끼우고 재실행하면 끝.
  - ① 테두리 flood fill(이웃 대비 허용오차 9) — 부드러운 그라데이션 배경을 따라가며 번진다
  - ② 도형 안에 갇힌 배경 주머니(리라 현 사이·말 다리 사이 등)는 주변 배경색 추정치(반지름 60 박스합)와 비교해 제거
  - ③ 1px 침식 + 페더로 배경색 후광 제거 → 어두운 밤하늘 위에서 테두리가 뜨지 않는다
  - ④ 알파 커버리지가 0인 가로 띠로 3행 분리, 각 행에서 넓은 세로 틈 순으로 6/6/5칸 분리 후 여백 트림
- 구조 변경: 시트 1장 + 크롭 좌표(`background-size:600% 300%`) → **별자리별 개별 이미지**(`CON_IMG[key] = data URI`, `background-size:contain`). 칸 경계·비율 왜곡 문제가 원천 소멸
- 총 381KB(base64 508KB) — 구 시트(610KB)보다 가볍다

### ② 2막 신설 — **계절이 도는 하늘**
- **막 구조**(`ACTS`): 1막 클리어 → "2막으로 →" 버튼 → 덱·유물·아이템·가루를 그대로 이어받아 2막 시작 (HP 15 회복, 가루 +80)
- **계절 순환**: 1막은 무작위 계절 고정, **2막은 턴마다 봄→여름→가을→겨울**. 시작 계절은 여정마다 무작위(`seasonOff`) — `curSeason()`이 `(turn-1+seasonOff)%4`
- **계절 가시화**(신규 UI): 전투 상단 = **사계 바퀴**(현재 계절 발광 + 다음 계절 예고) · 손패 카드 = **제철 +25% / 역계절 −25% 태그**(`.seastag`) · 맵 HUD = "계절 순환 — 봄→여름→가을→겨울"
- **2막 적 10종**: 쫄몹 = 천왕성·해왕성·명왕성·타이탄·이오·떠돌이 행성·오르트 파편 / 중간보스 = **게 성운 M1**(135/12)·**헤르쿨레스 구상성단 M13**(125/10, 소환형) / 보스 = **은하핵의 블랙홀**(170/15)
- **블랙홀의 중력**(신규 의도 `drain`): 4턴마다 "다음 턴 성간 물질 −1". 아머 14/3턴과 함께 3종 패턴(공격·성막·중력)을 돌린다
- 2막 보상 가루 1.5배 · 적 아트 10종 신규(외행성·구상성단·강착원반)

### ③ 별 150종 · 별자리 26개 (신규 9별자리 50종)
- **신규 9개**(계절 균형 목적 — 봄5·여름6·가을5·겨울5·주극5): 궁수(황도)·왕관·까마귀·게(황도)·마차부·페르세우스·케페우스·용·고래
- 별선은 전부 **실제 J2000 좌표 투영**(`gen_const_lines.py`에 좌표 추가 → `const_lines.json` 재생성). 궁수 = 찻주전자, 케페우스 = 오두막, 고래 = 머리 오각형 + 몸통 고리
- 아이덴티티: 궁수 = 겨눔(관통) · 왕관 = 대관(완성 보조) · 까마귀 = 약탈(버리기) · 게 = 집게(취약·약화) · 마차부 = 전차(성막→화력) · 페르세우스 = 처형 · 케페우스 = 인내(지속 성막) · 용 = 브레스(광역 화상) · 고래 = 삼킴(피해+회복)
- 신규 효과 타입: `aoeBurn`·`varBlock`·`halfBlockDmg`·`dmgExec`·`dmgKill`·`crown`·`sacBlock`/`sacMana`·`dmgMulti`의 `pierce` / 보너스 `needDownB`·`drawDiscard`·`vulnWeakAll`·`dmgRandBlock`·`execLowest`·`dmgHeal`
- 신규 별자리는 각인 시트에 없으므로 **수제 SVG 형상 9종**(`CON_FIG`)이 폴백으로 뜬다
- **관측 기록판 대응**: 덱 별자리가 26개까지 늘어 한 줄이 2200px로 터지던 문제 → **상위 9개만 표시**(완성 > 진행 중 > 완성 가능 > 손패 보유 순, 덱 순서로 정렬) + `+N` 칩(호버 시 나머지 진척)

### v0.18 검증 (로컬 서버 + 브라우저 스크립트, 런타임 에러 0)
- 데이터 정합: 별 150 · 별자리 26 · 선 데이터 26 · 각인 17(전부 data URI) · 형상/아이덴티티/도감순서 누락 0 · **si 인덱스 전수 유효** · 로어 누락 0 · 적 23종 아트 누락 0 · 카드 150종 기본판·강화판 전수 렌더 성공
- 신규 효과 실동작: `halfBlockDmg`(성막 6 → 피해 3) · `dmgExec`(HP 절반 이하 10+8, 역계절 −25% 적용 13) · `dmgKill`(처치 시 물질 2 환급) · `aoeBurn` · `varBlock` · **관통 다단**(적 성막 99 무손실 통과) · `crown`(완성 시 드로우) · `sacBlock`/`sacMana`
- 신규 보너스 실동작: 궁수(광역 관통) · 왕관(필요 수 −1) · 까마귀(뽑고 버리기) · 게(전체 취약+약화) · 마차부(성막만큼 피해) · 페르세우스(최저 HP 처형 + 물질·드로우) · 케페우스(성막 유지) · 용(광역 피해+화상) · 고래(피해+회복)
- 막 흐름: 1막 전투/중간보스/보상/상점/휴식/이벤트 → 1막 보스(M42 100) → **2막 전환**(계절 순환 on, 맵 재생성, 덱 유지) → 2막 전투(타이탄·명왕성) → 2막 보스(블랙홀 170) → 최종 클리어 화면
- 계절: 8턴 연속 전투에서 겨울→봄→여름→가을 순환 확인, 블랙홀 의도 3종(공격 13 / 성막 18 준비 / 중력 −1) 주기 확인, 중력 발동 후 다음 턴 물질 3→2
- 회귀: 튜토리얼(4단계 진행) · 단판 보스전(M42 원래 수치 140) · 도감 150장/26그룹 · 성좌 완성 연출(각인 그림) · 신규 별자리 SVG 폴백 · 기록판 9칸+17 확인

## v0.17 변경 (사용자 피드백 10건 — "빼는 개편" + 실제 그림)

- **실제 성좌 각인 그림 임베드** (사용자 제공 `성좌각인.png` = 1536×1024 WebP 스프라이트 3행×[6·6·5]칸): `embed_conart.py`가 base64 데이터 URI로 주입(매직 바이트 MIME 감지), `CON_IMG` 17칸 크롭 매핑. 사용처 = **성좌 완성 연출 형상 · 성좌판 배경 · 도감 헤더 썸네일**. 시트 없으면 CON_FIG SVG 폴백
- **관측 기록판** (구 카드형 미니 패널 폐지): `✦──카시오페이아 0/2──✦──큰곰 0/3──✦` 얇은 한 줄 — 활성 별자리 밑줄, 완성 가능 ★초록, 완성 금색, 진척 갱신 시 숫자 튀는 애니메이션
- **성좌판 다이어트**: 평상시 **완전 숨김**(적만 보임) → 카드를 내면 각인 그림 배경 + 별선 점등 + **"큰곰자리 1/3"만** 표시(설명 문구 전부 삭제 — 상단·카드와 중복 제거). 1성 남으면 ★ 경고
- **손패 발광 3단계**: 기본 프레임 놋쇠 톤다운(차분) / 낼 수 없음 회색 / **완성 직결만 금빛 펄스** — "모든 카드가 중요해 보이는" 문제 해소
- **카드 정보 직관화**: 성좌 기여를 우상단 소형 뱃지 → **아트 아래 전폭 스트립 "★ 큰곰 +1"**(10.5px 굵은 금색)
- **좌측 피드 축소**: 폭 174px·최대 4개·기본 한 줄 말줄임, 호버 시 전체 문구 확장
- **조력자 접기**: 평소 초상+이름 70px, 호버 시 능력 설명 확장
- **성막 자원 승격**: 에너지 오브와 같은 급의 **푸른 오브**로 (물질·성막·HP 한 줄)
- **연출 순서 완성**: 비공격 카드는 **성좌판의 해당 별 위치로 날아가고**, 별 점등 pop·선긋기·점등음이 0.28s 지연 — "카드 도착 → 별이 찍힘 → 선 연결 → 기록판 갱신" 순서감

## v0.16 변경 (사용자 상세 피드백 — "별자리 연결을 주인공으로")

- **중앙 성좌판**(`#conStage`, 밤하늘 패널 중앙): 카드를 낼 때마다 그 별자리가 **대형(224px)으로 그려지고 해당 별이 점등**, 새 별과 이어지는 선은 stroke-dash 선긋기 애니메이션 + 수정음(`sfx star`). 형상 일러스트가 각인처럼 뒤에 깔림(참조 이미지 스타일 — 형상+별선 통합). 라벨: "큰곰자리 2/3 · 한 턴에 3성을 이으면 → …" / 1성 남으면 초록 경고 / 완성 시 금빛 발광
- **카드-성좌 연동**: 카드 우측에 **기여 뱃지**(`큰곰 +1`, 미자르 `+2`, 알페라츠 겸용 표기). **이 카드로 지금 완성 가능하면 카드가 금빛 펄스**(`willfinish`) + 상단 미니 패널도 초록 "완성 가능!" 펄스(`ready`)
- **완성 연출 강화**: 화면 전체 골드 플래시 + 형상 불투명도 상향(.42→.68) + 완성 벨 5음+샴머로 보강
- **가독성/밀도**: 보드 1160→**1320px**, 카드 효과문 10px·굵기 500, 로어 8px, 패널 라벨 볼드, 스탯 수치 17.5px, 기능 버튼(pickbtn) 13px·금테 확대
- **성좌 효과 표기 정리**: "✦ 성좌 완성 · 3성 — …" → **"✦ 한 턴에 큰곰 별 3개를 이으면 → 성막 8 + 가시 3"** (조건→보상 문장)
- **맵 별자리 컨셉**: 노드에 **상시 한글 라벨**(전투/중간보스/상점/휴식/이벤트/보스) + 글로우 — 픽토그램 상징성 지적 해소
- **도감**: 별자리마다 플레이 스타일 태그 표기(`CON_IDN` — "성벽 — 방어와 가시" 등 17종)

## v0.15 변경 (사용자 피드백 9건 — 승인 플랜)

- **성좌 형상 17종 신화풍 재작**: 스틱 피겨 → 다중 패스 신화 실루엣(곤봉·방패 든 오리온, 포효하는 큰곰, 옥좌의 카시오페이아, 창 든 쌍둥이, 날개 편 처녀, 사슬의 안드로메다 등) + 공통 장식(점선 후광 링 + 월계 가지) — **사용자 평가 대기 2차 시안**
- **조력자 선택 = 증강 스타일**: 대형 카드 3장(232×352, 초상 후광·세리프 이름·능력 뱃지) 시차 등장 연출 + 호버 발광. **발동 이펙트** 신설(`allyFX`) — 초상 발광 펄스 + 갈릴레오 금빛 빔(조력자→적) / 브라헤 성막 물결 / 허셜 혜성 스파클
- **좌측 효과 발현 피드**(`#fxFeed`, 중앙 로그 바 폐지): 카드 사용 = 별선 아트 썸네일 칩, 성좌 완성 = 금테+형상 칩, 조력자 = 초상 칩, 피격 = 적색 칩. 최대 6개·6초 후 페이드. `log(msg,opts)` 하위호환 라우팅(숨은 #log에 전체 이력 유지)
- **손패 FLIP 전환**: 재배치 시 이전 위치에서 미끄러지고, 드로우 카드는 덱 더미에서 날아들며 등장(uid 추적)
- **적 일러스트 13종 재작**(`ENEMY_ART`): M42 날개 성운+트라페지움, 플레이아데스 7자매+청색 성운기, 히아데스 V성단, 행성 5(크레이터·구름 밴드·극관·대적점·이중 고리), 위성·혜성 이중꼬리·수정 파편·소행성. art 창 88→108px
- **카드 텍스트 문장형**: "피해 6" → "적 하나에게 피해 6을 준다" + 키워드 부연(중독·취약·가시 등 괄호 설명). **별 로어 100종**(`LORE`) — 실제 천문학 기반 한 줄, 카드 하단 이탤릭(카드 높이 260→272)
- **게임판 웅장화**: 패널 이중 금테 + 4귀퉁이 장식(`cnr4`) + 성운 텍스처, '밤 하 늘' 라벨 중앙 세리프, **턴 종료 = 금장 인장 원형 버튼**(78px)
- **성좌 미니 패널 확대**: 64→100px (별선 66px, 라벨 11.5px)

## v0.14 변경 (사용자 지시 4건)

- **성좌 발동 = 턴당 성좌마다 1회** (다중 완성 폐지 — 북극성 −1 연쇄 밸런스 붕괴 수정, 밸런스로그 11차). 검산: 카프 4장 = 카시오페이아 1회, 북극성+페르카드 연타 = 작은곰 1회
- **박진감 브금**: 앰비언트 드론 → **112BPM 시퀀서** (킥 정박/약박 + 하이햇 + A단조 베이스 아르페지오(Am–F–C–G) + 트라이앵글 리드 + 별빛 하이라이트). 효과음 9종 유지
- **일러스트 1차 시안** (수제 SVG 라인아트 — **사용자 평가 대기**): ① 천문학자 초상 3인(갈릴레오 대머리+수염, 브라헤 플랫캡+콧수염+러프 칼라, 허셜 보닛+곱슬) — 조력자 선택 화면(48px)·전투 조력자 박스(54px) ② **별자리 형상 17종**(`CON_FIG` — 사냥꾼·곰·왕관·리라·백조·독수리·전갈·사자·쌍둥이·황소·처녀·목동·천마·사슬 여인·개·물고기) — 성좌 완성 연출의 형상 단계(아트가이드 4단계 中 3단계)와 도감 그룹 헤더에 적용
- **이모지 완전 제거**: 계절 메달(벚꽃·태양·단풍·눈꽃·북극성 SVG 문장), 적 의도(검·방패·반짝 SVG), 적 상태줄(색 텍스트), 맵 힌트·도감 헤더 — 남은 기호는 ✦·♪ 등 타이포그래피 문자만

## v0.13 변경 (사용자 지시 7건 — 부재 중 자율 작업)

- **별 100종** (+48, 카드목록_v0.8.md): 별자리별 아이덴티티 통일 — 오리온 = **램프**(낸 오리온 수만큼 강화, `battleCounts` 전투 누적) · 큰곰 = 방어(성막+가시) · 카시오페이아 = 공격(셰다르를 매 턴 성막 → **매 턴 무작위 적 피해 3** `powerAtk`로 개정) · 큰개 사냥 · 쌍둥이 다단 짝 · 사자 포효 광역 등 17종 전부. 신규 내부 효과: `blockPerCon`·`everCon`·`powerAtk`·`seal`(단일 카드)
- **성좌 완성 = 한 턴 기준 회귀** (사용자 확정): v0.10의 전투 누적 폐지, 같은 턴 다중 완성 유지. 램프 효과만 `battleCounts`로 전투 누적
- **전투 중 덱/버림 즉시 열람**: 덱 더미(카드 뒷면)·블랙홀 클릭 → 카드 그리드 (덱은 이름순 정렬로 순서 은폐)
- **손패 팝업 반복(플리커) 구조 수정**: 호버 판정을 **고정 슬롯**(132×212)으로 분리, 변형은 안쪽 카드만 — 확대된 카드가 커서를 벗어나도 판정 불변이라 반복 팝업 원천 차단
- **탈이모지 아트 (AI 느낌 제거)**: 아이템 7·유물 6·조력자 3·맵 노드 6종을 **수제 SVG 라인 아이콘**(`ART` 사전, 금선 스타일)으로 전면 교체. 성좌 완성 연출의 이모지 형상 → 금색 장식 링+광선 문장. **이벤트 7종에 수제 SVG 비네트**(유성우·떠도는 별·검은 구멍·성운·보관소·행상 수레·관측소 돔). 상점·휴식·보상·이벤트 화면에 ✦ 장식 괘선(`ornline`). 로그·버튼·스탯 박스의 이모지 제거
- **오디오** (사용자 추가 지시): 웹오디오 신디사이즈 — 외부 파일 없이 **브금**(55Hz 드론 + 펜타토닉 별빛 스파클 앰비언트) + 효과음 9종(카드 사용·타격·피격·드로우·성좌 완성 벨·아이템·구매·승리·패배). 첫 클릭에서 AudioContext 기동(자동재생 정책), HUD "♪ 소리" 토글, 동일 효과음 60ms 스로틀

## v0.12 변경 (사용자 지시 — 문서 미준수 정정 포함)

- **게임명 Starry Night 전면 반영** (구 가칭 "성좌 전쟁" 폐기 — 타이틀·HUD·메뉴). ※ CLAUDE.md에 이미 있던 이름을 빌드에 반영하지 않았던 것 — 팀 공통 원칙 2번(작업 전 md 필독) 신설로 재발 방지
- **조력자 = 천문학자 위인 3종** (기획서 설정 복원 — 임의 창작 "꼬마곰 폴라" 폐기): 🔭 갈릴레오 갈릴레이(턴 종료 시 무작위 적 별빛 3) · 📐 튀코 브라헤(매 턴 시작 시 성막 2) · ☄️ 캐럴라인 허셜(짝수 턴 시작 시 +1장) + 홀로 출발
- **전체화면 레이아웃**: 보드(밤하늘·효과 발현 창·관측대)를 중앙 1160px 컬럼으로 제한 + **관측대를 화면 하단에 고정**(`margin-top:auto`) — 와이드 화면에서 텅 비어 보이던 문제 해소
- **덱 더미 = 별자리 카드 뒷면** (작은곰 별선을 금테 카드백에 새김, 겹침 그림자로 더미 표현) / **버림 더미 = 블랙홀** (강착원반 회전 애니메이션)
- **손패 반응 조율** ("뻑뻑함" 지적): 지연 0.13→0.05s, 확대 1.34→1.42, 부드러운 cubic-bezier 곡선
- **UI 톤 정리** ("AI 같음" 지적): 타이틀·화면 제목 세리프체(Georgia/바탕), 알약형 라운드(20px) → 각진 정제 보더(6~8px), 유리알 배경 → 잉크빛 반투명, 메뉴 제목에 ✦ 장식

## v0.11 변경 (사용자 지시 5건 — 와이어프레임 반영)

- **전투 UI 전면 재배치** (사용자 스케치 기준): 계절 배지 좌상 고정 · 아이템/유물 트레이 우상 고정 · 적 중앙 상단 · **효과 발현 창**(구 로그를 적 바로 아래 가로 바로) · 중단 스탯 줄(성간 물질 오브 · ❤ HP · 🛡 성막 박스) · 하단 = 덱 더미(좌) + 부채꼴 손패(중) + 버림 더미(우) · 조력자 박스 우측 고정
- **회전하는 계절의 밤하늘**: 배경 캔버스를 rAF 애니메이션으로 — 별 240개가 천구 북극(화면 위 너머)을 중심으로 느리게 일주운동, **현재 계절의 별자리 3개**(CONST_LINES 실좌표)가 금색 실루엣으로 함께 회전. 계절이 바뀌면 페이드 전환 (`SEASON_CONS`·`setBgSeason`)
- **손패 호버 감도 완화** (사용자 지적 "계속 팝업"): 확대 1.5→1.34배, 상승 148→112px, **호버 진입 지연 0.13s** (스치면 안 뜨고, 잠시 머물러야 확대)
- **아이템 이원화**: 소모품 7종(별빛 소나기 = 뽑기 2, 수축 렌즈 = 전체 약화 1턴 신규) + **패시브 유물 6종** 신설 — 북두의 나침반(전투 시작 성막 5)·별지기의 렌즈(첫 턴 +1장)·살아있는 성화(매 턴 첫 공격 +2)·은하수 물병(휴식 +10)·유성 부적(전투 시작 무작위 적 5)·별먼지 자석(보상 가루 +10). 획득처: 상점 유물 슬롯(60~80 가루) · **중간보스 격파 드랍** · 이벤트 "버려진 관측소"
- **조력자 시스템**: 런 시작 시 선택 화면(꼬마곰 폴라 = 매 턴 종료 시 무작위 적에게 별빛 3 / 홀로 떠난다). 전투 화면 우측 박스 표시, 맵 HUD에도 표기. 튜토리얼·단판에는 미적용

## v0.10 변경 (사용자 지시 6건 + 확정 답변 3건 — 밸런스로그 8차)

- **시작 덱 = 주극성 12장** (`POLAR_DECK`): 카프×2·치흐·두베×2·메라크·알리오트·미자르-알코르·코카브×2·페르카드×2 (희귀 셰다르·북극성 제외). **계절 테마 선택 폐지** — 1막 계절은 무작위 고정, 맵 HUD에 표시
- **성좌 카운트 전투 단위 누적** (사용자 확정): `startTurn`의 turnCounts·litSi·completed·compCount 리셋 제거 — 턴이 지나도 진행 유지, 별 점등도 전투 내내. 북극성(needReduce)·알타이르(playedTurn)는 턴 단위 유지. 벨라트릭스·튜토리얼 문구 개정
- **덱 보기** (`showDeckView`): 맵 HUD 필("🃏 덱 N장 보기")·상점·휴식·보상 화면에서 현재 덱을 카드 일러스트 그리드로 열람
- **전투 모션 1차**: ① 카드 사용 시 카드가 대상으로 날아가는 비행 클론(`flyCardFX`) ② 적 피격 셰이크+플래시(`hitfx`, `S.fxHits` 기록→render 부여) ③ **적 턴 순차 연출** — `endTurn`을 setTimeout 체인으로 리팩터: 적 하나씩 돌진(`lunge`)→피해→관측대 셰이크+붉은 비네트(`playerHitFX`), 진행 중 `S.busy`로 입력 차단
- **하스스톤식 손패**: 부채꼴 배치(회전 ±4°/장·호 곡선·겹침, scale 0.82) + 호버 시 1.5배 확대·전면 상승(`@media hover` — 모바일은 탭=사용 유지). 손패 전용 `.hslot` 클래스로 도감·상점 카드와 분리
- **적 아머** (보스+중간보스, 사용자 확정): `defend:{every,v}` 데이터 — M42 3턴마다 성막 12 / 플레이아데스 4턴마다 8 / 히아데스 3턴마다 10. 방어 의도 "🛡 성막 N 준비", 다음 자기 행동 전까지 유지. `hit(e,dmg,pierce)` — **관통(아다라·독수리 보너스)이 아머 무시**, 중독·화상 도트도 무시. 상태줄 🛡 표시

## v0.9 변경 (사용자 지시 5건 — 밸런스로그 7차)

- **맵 확장 (관문 약 10개)**: 3열×5행 → **3열×9행** + 보스 전 휴식 + 보스 (29노드). 노드 배분: 전투 ~15 · 중간보스 3 · 상점 2 · 휴식 3+1 · 이벤트 4. 조우 3티어(0~1행 쉬움 / 2~5행 보통 / 6행~ 강함, `ENC_HARD` 신설)
- **아이템 시스템 (소모품 5종, 런 전용)**: 성운수(HP 15)·유성 조각(단일 12)·별빛 부적(성막 10)·시간의 모래(물질 +2)·초신성 조각(전체 8). 최대 3개 소지, 전투 중 관측대의 아이콘 버튼으로 사용. **상점 강화** — 카드 3장 + 아이템 2종 + 카드 제거. **아이템 이벤트 2종** 추가(유실물 보관소·행상 별지기, 소지 초과 시 가루 +20 전환)
- **강화·제거 픽커를 카드 일러스트 그리드로 교체** (텍스트 버튼 폐지). 강화된 카드는 흐림 처리
- **쫄몹 전면 강화** (사용자 지시 "너무 약함"): 수성 14/4→22/5 · 금성 24/5→34/6 · 화성 28/7→40/8 · 목성 45/6→60/7 · 토성 34/5→48/6 · 위성 26/5→38/6 · 혜성 42/7→55/8 · 파편 16/4→24/5. 중간보스도 상향: 플레이아데스 60/7→80/8, **히아데스 성단 신설**(95/9, 패턴 9·9·13)
- **중간보스 상시 배치**: 구간별 1개 — 초반(2~3행)·중반(4~6행)·후반(7~8행) 3회, 종류는 성단 2종 중 무작위. 격파 보상 가루 50~70

## v0.8 변경 (사용자 지시 정정 반영 — 밸런스로그 6차)

- **적 위계 정리**: 쫄몹 = 행성·위성·혜성·파편 통합 풀(수성·금성·화성·목성·토성 + 미아 위성·얼음 혜성·성운 파편 복원) / **보스급 = 딥스카이** — 중간보스 **플레이아데스 성단**(60/7, 아기별 소환, elite 풀사이즈 표시) 신설, 최종보스 M42 성운 유지
- **중간보스 노드**: 맵 3~5행의 전투 칸 1개가 ✸로 승격. 보상 상향(가루 45~60 + 카드 3택1). 소환 로직 일반화(`minionMax` 있는 적 = 소환형)
- **1막 기본덱 4종 (계절 테마 선택)**: 봄(사자+목동) / 여름(거문고+독수리) / 가을(페가수스+안드로메다) / 겨울(오리온+큰개 — 기본별과 동계절). 각 16장 = 계절 별자리 2개 + 기본별 8종 + 보충 2장
- **1막 계절 고정**: 계절 순환 없음 — 선택한 테마의 계절로 전투 내내 고정(`S.fixedSeason`, HUD "1막 고정" 표기). 테마 별자리 카드는 상시 제철 +25%. 튜토리얼·단판 보스전은 기존 순환 유지

## v0.7 변경 (사용자 지시 4건 — 밸런스로그 5차)

- **성좌 다중 완성**: "별자리별 턴당 1회" 폐지 — 필요 수를 다시 채울 때마다 같은 턴 재발동 (`S.compCount` 도입, 로그 "완성 ×N!", 패널 "완성 ✦×N"). 북극성 필요 수 감소와 자동 결합
- **1막 기본덱 고정 시작**: 프리셋 선택 제거 → `BASIC_DECK` 16장 (거문고 3 + 독수리 3 + 기본별 8 + 카프·두베 1장씩 추가). 단판 보스전은 프리셋 4덱 유지
- **일반 몹 = 행성 5종**: 수성(14/4)·금성(24/5)·화성(28/7, 패턴 7·7·9)·목성(45/6)·토성(34/5, 패턴 5·5·8) — 성운 파편·얼음 혜성·미아 위성 폐기. 행성별 SVG 아트
- **1막 보스 하향**: `ENEMIES.m42.act1 = {hp:100, atk:9}` — 1막 보스 노드에서만 적용(`atkOv`), 단판·튜토리얼은 원래 수치

## v0.6 변경 (사용자 지시 반영)

- **1막 여정 (StS식 무작위 맵)**: 3열×7행 갈림길 — 전투 9 · 상점 1 · 휴식/강화 3(보스 전 휴식 고정 포함) · 이벤트 3 → 보스 M42. 매 런 무작위 생성, 인접 열로만 전진(**되돌아가기 불가**), 지나온 길은 금색 실선 표시
- **런 시스템**: HP·덱·별빛 가루가 노드 간 유지. 전투 승리 보상 = 별빛 가루 + 별 3택1(스킵 가능). 사망 시 런 종료
- **상점 (별빛 가루)**: 별 3종 판매(일반 40/희귀 65/전설 110) + 카드 제거 50 (방문당 1회)
- **휴식/강화**: 휴식 HP +20 또는 카드 1장 강화(+). **강화는 프로토타입 임시 일괄 규칙** — 명시 특례(북극성 등 7종 비용 −1) / 수치형 +25% 올림(다단히트는 총합 +25%) / 약화·취약 +1턴 / 그 외 비용 −1, 0코 유틸은 뽑기 +1. 카드별 확정 표는 밸런스팀 추후
- **이벤트 4종**: 유성우(가루 +40) · 떠도는 별(무작위 카드) · 검은 구멍(HP −8 ↔ 가루 +60) · 고요한 성운(HP +10)
- **일반 몹 3종 신설(임시 수치)**: 성운 파편(16/4) · 얼음 혜성(42/7, 패턴 7·7·10) · 미아 위성(26/5, 패턴 5·8) + 기존 소행성. 조우 풀: 1층 쉬움 2종 / 이후 4종
- **밸런스 v0.7 반영 (사용자 확정)**: 전갈 완성 중독 5→3, 페가수스 4성→3성(보너스 물질 2+뽑기 1), 큰곰 완성 성막 8+가시 3(`blockThorns`), "제물"→**"버리기"** 전면 개칭
- **큰곰자리 별선 교체 (사용자 지시)**: 북두칠성 7성 → **큰곰 전신 18성** (Stellarium modern 표준 연결, J2000 투영 — `gen_const_lines.py` 재작성·`const_lines.json` 갱신). 카드 si 인덱스는 앞 7성 순서 보존으로 무변경
- v0.5.1의 costDown 버그 수정 포함

## v0.5.1 변경 (버그 수정, 수치 변경 없음)

- **비용 할인 출처 미구분 버그 수정** (`effCost`/`playCard`): 기존 `if(cost<d.cost)S.costDown--`는 폴룩스 복사본의 "이번 턴 비용 −1"(tempTurn) 할인까지 셰아트류 costDown 소비로 오인 → costDown 0에서 복사본을 내면 costDown이 −1이 되어, 이후 같은 턴 셰아트의 +1이 상쇄되며 다음 카드 할인이 무효가 되는 문제. 수정: `baseCost()`(tempTurn만 반영)를 분리하고 `cost<baseCost(d,c)`일 때만 costDown을 차감
- 부수 교정: 복사본이 이미 0코스트라 costDown 할인이 실제로 적용되지 않은 경우, costDown을 소비하지 않고 다음 카드로 이월 (동일 근본 원인 — 효과 없는 할인 소비)

## v0.5 변경 (사용자 지시 반영)

- **밸런스 v0.6 전면 반영**: 희귀도 파워 예산(기본 100 / 일반 110 / 희귀 125 / 전설 145%), 별자리마다 다른 완성 필요 수(1성×1 · 2성×7 · 3성×8 · 4성×1), 성좌 보너스 17종 전부 고유, 카드 52종 효과 전부 상이
- **신규 키워드 구현**: 취약(받는 피해 1.5배), 화상(고정 도트 2턴), 가시(피격 반격), 관통(표기), 봉인(적 의도 취소), 변광(무작위 수치), 지연 드로우, **제물**(선택적 손패 버리기 → 추가 효과, 배경 클릭 = 생략), 강제 버리기(코카브), 폴룩스 복사본 이번 턴 비용 −1
- **특별 성좌 제거**: 대삼각형·대사각형 삭제 (추후 퀘스트로 이관 예정). 알페라츠 페가수스 겸용 판정은 유지
- **성좌 진행 UI 교체** (사용자 지시): 상단 "~자리 1/2" 칩 폐지 → **별자리 미니 패널에 낸 별이 하나씩 점등**, 완성 시 별선 전체가 금색 점등 + "완성 ✦"
- **카드 판(보드) 디자인**: 적 영역 = "밤하늘" 패널, 손패·자원 영역 = "관측대" 패널로 프레임 분리 (금테, 라벨)
- 프리셋 덱 개편: 특별 성좌 덱 → "가을 요새"(페가수스 4성 + 안드로메다 봉인) 교체
- 튜토리얼 문구를 별자리별 필요 수 기준으로 갱신

## 기술 결정 (누적)

- 단일 HTML, 외부 의존성·localStorage 없음. 데이터 주도: `STARS`/`CONSTELLATIONS`/`ENEMIES`/`CONST_LINES`(실좌표 J2000 투영, `제작/const_lines.json`)
- 계절 보정 `seasonAdj()` 일원화 (피해·성막·중독·화상 수치만, 다단히트는 총합 기준), 성좌 판정은 사용 후 전 별자리 재검사(북극성 순서 무관)
- 성좌 완성 4단계 연출: 점멸 → stroke-dashoffset 선 잇기 → 형상 그림(임시 이모지) → 배너, 큐+축약

## 검증 (Playwright)

- v0.5: JS 에러 0. 시나리오 검산: 베텔게우스 제물(+5) → 리겔 → 벨라트릭스로 오리온 3성 완성(보스 140→111, 보너스 피해 9+화상 2 확인), 점등 패널 5개 렌더, 도감 52종, 튜토리얼 시작, 가을 요새 덱 3턴 자동 진행 안정.
- v0.5.1 (환경에 Playwright 부재 → 내장 Chromium 브라우저 판에서 동일 절차 수행): JS 에러 0. 할인 시나리오 5케이스 전부 통과 — ① costDown 0에서 복사본(2코스트→1) 사용 후 costDown 0 유지 ② 같은 턴 셰아트 → costDown 1 ③ 다음 카드 1코스트→0으로 소비 ④ 0코스트 복사본이 costDown 미소비(이월) ⑤ 일반 카드가 잔여 costDown 정상 소비. 겨울 덱 3턴 자동 진행(보스 140→80), 도감 52종 렌더.
- v0.6 (내장 Chromium): 런타임 에러 0. 시나리오 — 맵 생성(17노드·간선 24·출발 3택), 전투 진입→승리→보상(가루+3택1), 상점(구매·제거·잔액), 휴식/강화(리겔→리겔+, 강화 수치 5+3 확인), 이벤트 4종 중 유성우, 보스 노드→1막 클리어 화면, 큰곰 완성 = 성막 19(8+3+보너스 8)+가시 3, 버리기 흐름(베텔게우스 6→+5) 문구 포함, 단판 보스전·튜토리얼 회귀, 도감 52종 + 두베 카드 18성 아트.
- v0.7 (내장 Chromium): 런타임 에러 0. 기본덱 16장·7별자리 구성 확인, 1막 첫 전투 = 행성(금성), 카프 4장 → 카시오페이아 완성 ×2(로그·패널 표기), 포말하우트 2장 → 1성 ×2 발동, 1막 보스 100HP·공격 9 / 단판 140·12 분리 확인.
- v0.8 (내장 Chromium): 런타임 에러 0. 테마 선택 4종 렌더·4덱 전부 16장, 봄 테마 계절 고정(턴 3에도 봄, 레굴루스 12→15 상시 제철), 중간보스 노드 1개 생성·플레이아데스 60HP·아기별 소환 실행·성단만 죽으면 전투 지속·전멸 시 "성단 격파!" 보상(가루 52), 단판 보스전 계절 순환 유지, 조우 풀 easy 3종·normal 7종 확인.
- v0.9 (내장 Chromium): 런타임 에러 0. 맵 11행 29노드(전투 15·엘리트 3·상점 2·휴식 4·이벤트 4·보스 1)·viewBox 880 스케일, 아이템 3종 전투 사용(성운수 +15HP·모래 물질 +2·초신성 전체 8, 슬롯 감소·버튼 렌더), 강화/제거 픽커 카드 일러스트 16장(강화된 카드 흐림·SVG 별선 포함), 상점 카드 3+아이템 2+제거 동작, 8행 전투 = hard 조우(혜성+수성), 엘리트 풀 2종 로테이션, 이벤트 아이템 획득·가득 시 가루 +20 전환, 튜토리얼 회귀.
- v0.17 (내장 Chromium): 런타임 에러 0. 시트 610KB·17칸 매핑·1536×1024 WebP 실디코드 확인, 기록판 한 줄(활성 밑줄·bump·★·완성), 성좌판 평시 숨김→각인 배경+«큰곰자리 1/3»만, ★스트립·차분 프레임·willfinish 1장, 파란 성막 오브, 조력자 70px 접힘, 완성 연출 fig=각인 그림+배너, 도감 각인 썸네일·100장, 튜토리얼·단판(140/12) 회귀.
- v0.16 (내장 Chromium): 런타임 에러 0. 성좌판 흐름 검산(빈 힌트 → 두베 "큰곰자리 1/3"+형상 → 2/3 "1성 남았다" → 미자르 완성 done+피드 금테 칩), 카드 뱃지(큰곰 +1/+2), willfinish 1장·패널 ready 1개 동시 점등, 맵 라벨 29개 전수, 도감 아이덴티티 태그·100장, 보드 확대, 튜토리얼·단판(140/12) 회귀.
- v0.15 (내장 Chromium): 런타임 에러 0. 증강 카드 3장(후광·뱃지·시차 연출), 좌측 피드(카드 아트 칩·완성 금테 칩·형상 포함·전투 시작 자동 칩), 손패 슬롯 uid 전수 + 허셜 2턴 6장 FLIP 진입, conmini 100px, 로어·문장형 렌더(도감 100장 로어 100개), 적 아트 SVG 전수, 원형 턴 종료·코너 장식 8개, allyFX 3종(빔·물결·발광) DOM 검증, 신화 형상 17종+월계, 튜토리얼·단판(140/12, 프리셋 세로 리스트 복원)·도감 회귀.
- v0.14 (내장 Chromium): 런타임 에러 0. 성좌 턴당 1회 검산(카프 4장 → 완성 로그 1개, 북극성 −1 + 페르카드 연타 → 작은곰 1회), CON_FIG 17종 전 별자리 매칭, 완성 연출 형상·도감 헤더 형상·초상 3인·조력자 박스 초상·카드 메달 계절 문장 렌더, 의도 SVG·상태줄 이모지 0, BGM 시퀀서 함수 확인(음향 자체는 실기기 청취 필요), 도감 100장·회귀 통과.
- v0.13 (내장 Chromium): 런타임 에러 0. STARS 100종·si 전수 유효·도감 100장 렌더, 한 턴 회귀 검산(1턴 두베+메라크 → 2턴 리셋 0/battleCounts 유지 → 미자르만으론 미완성, 같은 턴 3성 채우면 완성), 오리온 램프(메이사 2+1=3)·민타카 성막 4·셰다르 powerAtk 3+파워 라벨·뉴 봉인, 덱 열람 7장/버림 0장·닫기, 손패 슬롯 5개(슬롯 무변형·내부 카드만 scale — 플리커 구조 해결), 맵 노드 SVG·이벤트 비네트·장식 괘선 렌더, 오디오 함수 3종 존재(음향 자체는 실기기 확인 필요 — rAF/제스처 정책), 튜토리얼·단판(140/12) 회귀.
- v0.12 (내장 Chromium): 런타임 에러 0. Starry Night 표기 3곳, 조력자 선택 4항목·갈릴레오 사격/브라헤 매턴 성막 2/허셜 짝수 턴 6장 각 검증, 보드 폭 1160 중앙·관측대 하단 고정(바닥 여백 10px), 카드백·블랙홀 렌더, 회귀 통과.
- v0.11 (내장 Chromium): 런타임 에러 0. 레이아웃 검산(계절 좌상 14,44 · 트레이 우상 · 효과 발현 창 전폭 · 덱/버림 하단 양측 · 조력자 우측), 유물 훅 6종(나침반 성막 5·렌즈 6장·성화 매턴 +2·부적 개전 5·자석 +10 범위 검산·물병), 조력자 사격 로그·박스 표시, 상점 유물 슬롯 구매, 엘리트 유물 드랍 + 보상 문구, 신규 소모품 2종(뽑기 2·전체 약화 — 스위치 누락 발견 즉시 수정), bgTick 수동 프레임 회전 확인(rAF는 표시 중에만), 튜토리얼·단판(140/12)에 조력자·유물 미적용 확인.
- v0.10 (내장 Chromium): 런타임 에러 0. 주극 12장·전원 주극 확인·무작위 계절 고정 힌트, **성좌 누적 검산**: 1턴 두베+메라크(2/3) → 적 턴 체인 완주 → 2턴 미자르(2성 계산, 총 4) → 큰곰 완성·점등 3개 유지·패널 "완성 ✦", M42 3턴 "🛡 성막 12 준비"·일반 공격 아머 흡수·관통 8 무시 관철·상태줄 🛡 표시, 부채꼴 손패(absolute·±8° 회전·scale 0.82)·호버 규칙, 카드 비행 클론·피격 hitfx·관측대 셰이크·비네트, 덱 보기 4곳 열람/복귀, 튜토리얼·단판(140/12)·도감 52종 회귀.

## 알려진 한계 / 다음 할 일

- ~~신규 키워드 포함 전투 시뮬레이션 미실시~~ → **완료 (2026-08-06, 밸런스팀)**: `sim_battle_v1.py` 4덱×2,000판 — 결과·권고안은 밸런스로그 3차 기록 참조. 사용자 확정(4차 기록: 전갈 중독 3·페가수스 3성·큰곰 가시)은 v0.6에 반영 완료 — **v0.7 수치 기준 재시뮬레이션이 다음 밸런스 과제**
- 관통은 현재 적에게 성막이 없어 실효 없음 (적 성막 도입 시 활성)
- v0.6 신규 요소는 전부 **프로토타입 임시 수치**: 일반 몹 3종·조우 풀·별빛 가루 경제(보상 15~35/상점 가격)·이벤트 4종·강화 일괄 규칙 — 밸런스팀 검토 대기
- 강화는 v0.6에 구현됨(계승·합성은 사용자 확정으로 폐기). 아이템·조력자는 미구현
- 애니메이션팀 신설됨 (플러그인 v0.2.0) — 카드 사용/피격 모션은 애니메이션팀 몫

---

# v0.21 (2026-08-09) — 3막 · 도트 천체 · 조력자 수다 · 난이도 대폭 상향

사용자 지시 5건 + 버그 제보 1건.

## 0. 버그 수정 — 합성 카드가 전투에서 풀리던 문제

`startBattle()`의 덱 정규화(`cfg.deck.map(...)`)가 `{id, up}`만 복사해 **`id2`/`up2`를 버렸다**. 대장간 합성 카드가 전투에 들어가는 순간 앞쪽 별 한 장이 됐다(사용자 제보 "황도를 합성해도 하나로 치드라").
정규화·전투 덱 생성·복제(`copyRand`/폴룩스/임시 복제) 4곳을 모두 `id2`·`up2` 보존으로 고쳤다. 검증: 눈키·루크바트 합성 카드를 덱→손패 경로로 뽑아 `countAs=2`, 별 2개 점등(`si 1/12`), 한 장 더 내면 궁수 3성 완성.

## 1. 3막 — 이름 없는 것들

- `ACTS[3]` 신설. 쫄몹 7종(백색왜성·중성자별·마그네타·암흑 성운·초신성 잔해·유령 은하·마젤란 조각) + 중간보스 2종(M31·퀘이사 3C 273) + 보스 **암흑 물질**
- **막 고유 규칙 = 여명(`dawn`)**: 전투 14턴째부터 매 턴 **성막을 무시하는** 피해가 3, 5, 7…로 커진다. 턴 라벨에 "여명까지 N턴"으로 예고
- 3막은 **쫄몹까지 계절을 탄다** (1·2막은 딥스카이만)
- 신규 기믹 5종: `nullify`(무명 — 다음 턴 모든 성좌 완성 봉인) · `veil`(은폐 — 공격 수치를 ???로) · `jet`(제트 — 주기마다 성막 관통) · `creep`(청색 이동 — 매 턴 공격 누적) · `minionId`(소환체 종류 지정)
- 흐름: 2막 보스 격파 → 가루 +120 → 보스 유물 3택1 → **조력자 3인째** → 3막 → 암흑 물질 격파 = 진 엔딩

## 2. 막 규모 확대 · 대장간 축소

- 맵 3열×9행 → **3열×13행** (관문 11 → **15**), 행 간격 80 → 62px로 줄여 화면 높이 유지
- 특수 노드: 상점3·휴식5·**대장간1**·이벤트4 (사용자 지시 "대장간이 너무 많다" — 3 → 1). 합성은 휴식의 '합성' 선택지가 보완
- 중간보스 3회, 구간 [3-5][6-9][10-12] / 조우 티어 0-2 쉬움, 3-8 보통, 9+ 강함
- 휴식 회복 20 → **30**, 막 사이 회복 15 → **22** (관문이 늘어난 만큼 지속력 보정)

## 3. 적 = 도트(픽셀) 스프라이트 34종

`gen_pixel_art.py` 신설 — 절차적 생성 + 런렝스 SVG 주입. 자세한 규격은 아트스타일가이드 v0.8.

## 4. 조력자 대사 대폭 확대

- 상황 **15종**(개전·보스·성좌·대성좌·피격·위기·처치·승리·턴 잡담·계절 전환·봉인·적 아머·여명·맞장구·맵) × 7인 = **222줄** + 짝 대화 **35쌍 73줄**
- 발화 지점 추가: 턴 시작(75% 확률 잡담) · 계절 전환 · 손패 봉인 · 적 아머 · 여명 임박 · **3성 완성은 전용 대사(`big`)**
- 쿨다운 2턴 → **1턴**, 받아치기 확률 40% → **62%**, 3인이면 **세 번째가 끼어든다**(브라헤↔리비트 "자네 이름이 논문에 없다고?" → 세 번째: "괜찮지 않네. 내가 적어 두겠네.")
- 전투 밖에도 대화 — 맵·상점·휴식·대장간 화면에 `.allyquote` 칩

## 5. 난이도

쫄몹 +20%, 중간보스·보스는 대폭 상향 + 기믹 강화. 수치와 근거는 밸런스로그 15차. 요약: **1막 클리어(그리디 AI) 10.0% → 2.0%**, M42 아머 10→16·격노 상한 6→11, 블랙홀 210/16 → 330/23.

## 검증 (런타임 에러 0)

```
v0.21 | 별 195 · 별자리 26 · 적 34 · 픽셀 34
필요수 {"1":1,"2":16,"3":9} | M42(1막) 140/12 · 블랙홀 330/23 · 암흑물질 880/26
맵 13행 · {battle:23, elite:3, shop:3, rest:6, event:4, forge:1, boss:1}
튜토리얼 OK · 단판 보스전 OK · 도감 195장 · 카드 렌더 실패 0 · 픽셀 렌더 실패 0
합성 카드: 덱→손패 경로에서 countAs=2, 별 2개 점등, 궁수 3성 완성 확인
3막 보스: 의도 주기 atk atk def nullify atk def atk nullify def / 은폐 "공격 ???" / 여명 13~17턴 3,5,7,9,11
퀘이사 제트: 성막 40 보유 상태에서 HP −29 (성막 미소모)
M31: 6턴간 공격 22 → 30 (청색 이동) · 위성 은하 소환
전 구간: 1막 15관문 → 2막 15관문 → 3막 15관문 → "여정 완주 — 3막 클리어!" (조력자 3인)
```

---

# v0.22 (2026-08-09) — 성좌 완성 연출 재제작 · 조력자 갱신

사용자가 폴더에 넣은 `Constellation Effect.dc.html`(연출 레퍼런스)과 새 `조력자.png` 반영. "우선 이것만" 지시라 범위를 넷으로 한정했다.

## 1. 성좌 완성 연출 — 레퍼런스의 모션·삽화 배치로 교체

- **`gen_fx_lines.py` 신설** → `CONST_FX` 주입(26종 12.4KB). 레퍼런스의 `project()`를 그대로 옮겼다: 별자리 중심 입체 투영 → 320×300 상자 자동 맞춤 → 등급 기반 별 반지름
  - 레퍼런스에 있는 17종은 그 파일의 RA/Dec/등급 데이터를 그대로 사용
  - 나머지 9종(궁수·왕관·까마귀·게·마차부·페르세우스·케페우스·용·고래)은 `const_lines.json`의 100×100 좌표를 같은 규칙으로 옮겨 담았다
- **카드 데이터 불변**: 별 카드의 `si`는 여전히 `CONST_LINES`를 가리킨다. 연출용 기하를 따로 둔 이유 = 레퍼런스의 별 목록이 우리 것과 개수·순서가 달라 `si`를 재매핑하면 195장이 전부 흔들린다
- `pumpFX()` 재작성 — 별 3중 원 팝 → 선 순차 드로우 → 삽화 페이드인. 삽화는 각인 그림(17종) 또는 `CON_FIG` SVG(9종)
- 검수: 26종 전부 렌더 성공, 최종 프레임 시트 `성좌연출_v0.22.png`로 별-삽화 정렬 확인

## 2. 조력자 초상 교체

새 `조력자.png`는 흰 배경·한글 라벨이 사라지고 **갈색 그라디언트 배경**이 됐다. `embed_assets.py`의 행/열 밴드 분리가 무의미해져 **전경 연결 성분 7개를 직접 검출**하는 방식으로 교체(배치에 의존하지 않는다).

## 3. 조력자 박스 라벨

박스 맨 위에 금색 `조력자` 라벨 + 밑줄.

## 4. 능력 발현 대사

`barkBy(who, kind)` 신설 — 특정 조력자가 말하게 한다. 7인 × `skill` 4줄을 추가하고 능력 발동 지점 7곳에 연결:
갈릴레오(턴 종료 사격) · 브라헤(매 턴 성막) · 허셜(짝수 턴 드로우) · 케플러(3턴마다 물질) · 코페르니쿠스(손패 교체) · 허블(1턴 드로우) · 리비트(표준 촛불).
쿨다운은 기존 1턴 규칙을 따른다(매 턴 발동하는 브라헤가 대사를 독점하지 않도록).

## 검증 (런타임 에러 0)

```
v0.22 | 별 195 · 적 34 · 픽셀 34 · 연출기하 26 · 초상 7 · 배경 4
성좌 연출 26종 렌더 실패 0 (삽화 17 + SVG 폴백 9)
오리온: viewBox 0 0 320 300 · 선 11 · 별 10 · 삽화 78.8%×84.1% (지연 1.54s)
조력자 박스 라벨 '조력자' · 3인 표시 · 능력 대사 발화 확인
전 구간 1막→2막→3막 45노드 완주 · 튜토리얼 · 단판 보스전 · 도감 195장
```

---

# v0.23 (2026-08-09) — 별자리 데이터 통일 · 조력자 상시 표시 · 별자리별 완성 소리

## 1. "성좌판 별자리 ≠ 연출 별자리" 해소

v0.22에서 완성 연출만 레퍼런스 기하(`CONST_FX`)로 바꾸는 바람에 전투 중 성좌판(구 `CONST_LINES`)과 모양이 달랐다. 근본 해결:

- `gen_const_lines.py`의 투영을 **별자리 중심 입체 투영**으로 교체 (레퍼런스와 동일). 별 순서·개수 불변 → **카드 `si` 안전**
- `gen_fx_lines.py`가 `const_lines.json` 하나에서 **CONST_LINES(카드용 100×100)** 와 **CONST_FX(성좌판·연출용 320×300)** 를 함께 만들어 주입 → 별 인덱스 공유
- 전투 성좌판을 `CONST_FX`로 렌더 (viewBox 320×300, 삽화도 연출과 같은 상자). 카드 비행 목표 좌표도 같은 계로 변환
- 검증: 26종 전부 별/선 개수 일치, `si` 범위 초과 0건, 성좌판 첫 별 좌표 = CONST_FX 좌표

> RA/Dec를 보유한 15종(오리온·황소·쌍둥이·사자·처녀·큰곰·궁수·왕관·까마귀·게·마차부·페르세우스·케페우스·용·고래)은 새 투영으로 다시 그려졌고, 좌표가 없는 11종은 기존 배치를 그대로 320×300에 옮겨 담았다.

## 2. 조력자 — 어디서나 오른쪽 아래, 항상 펼침

- `#allyBox` 접기 폐지(호버 확장 삭제), 오른쪽 아래 고정(184px), z-index 24로 런 화면 위에
- `renderAllyBox()`를 `hideRunScreens()`에 물려 **맵·상점·휴식·대장간·이벤트·보상**에서도 그린다 (메뉴에서는 숨김)
- 손패 부채꼴이 박스를 침범하지 않도록 **슬롯 간격 상한 + 중심을 박스 폭의 절반만큼 왼쪽으로**. 조력자가 없으면 원래대로 중앙 정렬 (측정: 12장에서도 94px 여유)

## 3. 별자리별 완성 소리

`VOICE` 신디사이저 16종(포효·기합·울음·짖음·비명·독침·집게·활·베기·발굽·사슬·북·화염·고래·물방울·휘파람·화음·하프·차임)과 `CON_SFX` 표(26종). 웹오디오 합성이라 외부 음원 0.
소리는 **삽화가 떠오르는 시점**(선이 다 그어진 뒤)에 울려 연출과 붙는다.

## 검증 (런타임 에러 0)

```
v0.23 | CONST_LINES 26 · CONST_FX 26 · 소리 26종
별/선 개수 불일치 0 · si 범위 초과 0 · 카드 별자리 그림 si 오류 0
성좌판 viewBox 0 0 320 300 · 첫 별 cx=34 = CONST_FX 좌표
조력자 박스: 맵·상점·휴식·대장간·이벤트·전투 전부 표시(오른쪽 아래 184px, 설명 상시) · 메뉴에서 숨김
손패 5/8/12장 → 박스와 여유 160/112/94px · 조력자 없으면 중앙 복귀
전 구간 45관문 완주 · 튜토리얼 · 단판 보스전 · 도감 195장
```

---

# v0.24 (2026-08-10) — 손으로 잡는 카드, 옆에 선 사람, 한눈에 읽히는 하늘

사용자 피드백 11건. 순서는 서로 덜 부딪히는 쪽부터: 맵 → 멈블 → 계절/보상 → 병합 → 이름 → 조력자 → 드래그.

## 1. 맵 — 틀을 벗기고 위계를 세웠다
- `.mapnode`의 원형 테두리·배경·46px 고정 제거 → **아이콘만** 남기고 `NODE_VIS` 데이터로 색·크기 차등
  전투 30 은청 · 상점 32 금 · 휴식 32 초록 · 이벤트 32 하늘 · 대장간 34 주황 · **중간보스 50 보라+아우라** · **보스 70 적색+아우라**
- 근본 원인은 `_ic()`가 전부 `stroke="#e8c56a"` 단색이었던 것 → `.mapnode .svgic svg{stroke:var(--nc)}`로 CSS가 이기게
- 간격 `ROW_GAP` 62 → **82**, 맵 폭 400 → **520**, 열 x 66/200/334 → 80/260/440. 인접 노드 최소 간격 62 → **73px**
- 맵이 1230px로 길어져 현재 위치로 자동 스크롤
- 중간보스·보스 아이콘도 재작(성단 = 고리+별 무리 / 보스 = 이중 나선 + 뻗치는 선)

## 2. 조력자 멈블 사운드
`VOICE_TONE` 표 — 노년 남(갈릴레오·코페르니쿠스) 104~112Hz square · 중년 남(브라헤·케플러·허블) 138~152Hz · 여성(허셜·리비트) 232~246Hz triangle.
대사 길이 ÷ 3.2 만큼(3~10) 짧은 음을 인물별 음정 배열에서 골라 찍는다. `pumpBark()` 한 줄 호출로 모든 대사 경로를 덮는다.

## 3. 계절 가중 + 보상 무작위 3장
- `runSeason()`(다음 전투의 시작 계절) + `SEASON_BIAS`(제철 ×3 · 역철 ×0.5 · 주극 ×1.2)로 별자리 3택1을 가중 추첨
- **고르는 단계 폐지**: 별자리를 고르면 `rollConCards`가 희귀도 가중으로 3장을 뽑아 바로 담고, 무엇을 받았는지 결과 화면으로 보여 준다
- 희귀도는 **풀에 직접 가중치**(`GRADE_W`) — "등급 먼저 굴리기"는 별자리 안에서 못 쓴다(20개 별자리가 전설 0장). 상점·이벤트 추첨도 같은 `wpick`으로 통일

## 4. 합성 효과 병합
`FX_MERGE` 규칙표 + `mergeFx()`를 **`fusedData`에만**. 약화·취약은 합산이 아니라 `max`(적용이 `Math.max`라 합산하면 없던 버프가 생긴다), 피해는 관통 플래그가 같을 때만, 다단히트·전차·램프류는 금지.
부수 수정: 버리기 계열이 둘 다 `S.pend`를 세팅해 **뒤엣것이 앞엣것을 덮어써 효과가 조용히 사라지던 버그**, `FUSC` 캐시 키가 `up`+`up2`를 합산해 충돌하던 버그.

## 5. 카드 이름 체계 + 강화·합성 시각화
- **195장 전부에 그리스 문자(`by`) 부여**, 고유명이 없던 56장은 `name` 필드 제거 → 표시는 `작은곰자리 α (북극성)` / `황소자리 θ²`
- 제목칸이 113px뿐이라 `.cnm`(별자리 10.5px) + `.bay`(그리스 문자 16px 금색)로 쪼갰다. 부제는 `고유명 · 타입`. **도감 195장 전수 넘침 0**
- 강화는 이름에 `+`를 붙이지 않고 `upMark` 플래그 → **은청 프레임 + `+` 칩**, 합성은 **보라 프레임 + `쌍성` 칩**. 합성 이름은 별자리를 공유해 `작은곰자리 α·β`로 접는다
- 이름을 쓰던 12곳을 `cardName`/`cardShort`로 교정 — 이 중 4곳은 `STARS[c.id]`를 직접 읽어 **합성·강화 카드도 원본 이름으로 찍히던 기존 버그**

## 6. 조력자 누끼 레일
- `#allyBox`를 투명 컨테이너로 바꾸고 **오른쪽 가장자리 세로 레일**로. `.allysprite`(contain·바닥 정렬·`drop-shadow`)로 액자 제거
- 이름 12.5 → **14px**, 말풍선 12 → **14px**, 능력 설명은 호버 시 왼쪽으로 펼침
- 말풍선을 `#allyBox` 기준 → **`.allyone[data-who]` 본인 위**로. `allyFX`도 그 사람 기준으로(빔이 본인에게서 나간다)
- 전투/전투 밖에 복붙돼 있던 마크업을 `allyRailHTML()` 하나로 통합
- 손패 회피 계산을 정확히 다시 써 **부채꼴이 더 넓게 펴진다**(10장 기준 레일과 62px 여유)

## 7. 드래그 앤 드롭
- 기존 클릭 핸들러는 그대로 두고 `pointerdown`을 얹어 **8px 이상 움직였을 때만** 드래그로 승격 → `copy/sac/discard/choice` 모드는 손대지 않아도 클릭 방식 유지
- 고스트를 만들지 않고 **원본 카드에 transform만** 얹어 `.hslot`은 제자리 → FLIP 애니메이션이 그대로 산다
- 드롭 판정은 `elementFromPoint` → `.enemy`. 대상 없는 카드는 손패 바깥 아무 곳
- 드롭 시점에 `S.hand.indexOf(card)`로 **인덱스 재조회**(드래그 중 손패가 바뀔 수 있다), `touch-action:none`으로 모바일 스크롤 차단

## 이번에 밟은 함정 두 개 (기록)
1. **한 줄 주석이 뒤 코드를 삼켰다.** `document.body.addEventListener('click',()=>{...});`가 한 줄이라, 그 안에 `// 주석`으로 끝나는 줄을 끼워 넣자 나머지 `if(!S)return;...});`가 통째로 주석 처리돼 `Unexpected end of input`. **한 줄로 압축된 코드에 삽입할 때는 `/* */`를 쓰거나 줄바꿈을 보장할 것.**
2. **`dragClear()`가 `DRAG`를 이미 비운 뒤 불려** `.dragsrc` 클래스가 남았다 → 전역 쿼리로 지우게 수정.
이후로는 패치를 한 조각씩 넣고 매번 `node --check`로 파싱을 확인했다(`scratchpad/check.py`).

## 검증 (런타임 에러 0)
```
v0.24 | 별 195 · 바이어 누락 0 · si 오류 0 · 도감 195장 제목 넘침 0
맵: 노드 41 · 테두리 0 · 색 7종 · 아이콘 전투30/중간보스50/보스70 · 아우라 4 · 최소 간격 73px
보상: 3장 자동 획득 · 황소 1만회 등급 분포 기본28/일반49/희귀21/전설1.7%
계절: 봄 기준 3택1 3000회 → 봄 2707 vs 가을 454 (약 6:1)
병합: 전갈 합성 → 피해8·중독4·화상3 (각 1항목) · 쌍둥이 다단히트 2항목 유지 · upData fx 길이 불변
이름: '작은곰자리 α (북극성)' / '황소자리 θ²' / 합성 '작은곰자리 α·β'
카드: 기본/은청(강화)/보라(합성) 프레임 서로 다름 · 인장 각 1
조력자: 레일 투명 배경 · 3인 156×(185~193) · 이름 14px · 말풍선 본인 위 · 손패 겹침 없음
드래그: 대상카드→적 피해 적용 · 대상없는 카드→게임판 성막 · 손패 위 드롭=취소 · 잔여 스타일 0 · 클릭 방식 유지
전 구간: 1막→2막→3막 45관문 완주 · 튜토리얼 · 단판 보스전
```

---

# v0.25 — 별의 밝기가 곧 등급 (2026-08-10)

`전투프로토타입_v0.25.html` (약 1.24MB). 사용자 피드백 12건.

## 1. 카드 풀 195 → 111종 + 바이어 등급
- 남긴 장수: **3성 별자리 5장 · 2성 4장 · 1성 2장**. 선별은 "고유명 있는 별을 그리스 문자 순으로"
- `grade:'기본|일반|희귀|전설'` → **`rank:1~5`**(별자리 안에서의 그리스 문자 순위). `GRADE_NAME/CLR/ORD/W` + 상점 `prices` 전부 교체
- **효과 페이로드를 별자리 안에서 재배치**했다 — 별 고유 정보(`by·name·si·countAs·alsoCon`)는 별에 남기고, 효과 뭉치(`type·cost·target·fx·comp·extraTxt`)만 가치순으로 rank에 매핑. 별자리 총 전력이 보존돼 **보스 승률이 ±3%p 안에 머물렀다**(밸런스로그 17차)
- 수동 조정 2곳: 큰곰 5위 = 미자르-알코르(ζ, `countAs 2` 규칙 보존) · 전갈 5위 = 샤울라(λ)
- `LORE` 84개 제거, `POLAR_DECK`·`TUTORIAL_DECK`·`PRESETS`·`UP_COST_ONLY`의 죽은 id 전수 교정
- 도구: `scratchpad/p25_model.py`(파싱 + 페이로드 가치 모델) → `s25_1_cards.py`(선별·재배치·STARS 재생성) → `s25_1b_js.py`(rank 로직)

## 2. 카드 수급
- 시작 덱 12 → **9장**(주극 3별자리 × 3장, α 제외)
- 보상 = 뽑힌 3장 중 **담기/버리기 토글** (`REW` 상태 + `renderRewPick()`), 확정 시 덱 장수 변화를 보여 준다
- `rollConOffer`: 3택 중 **2개**를 보유 별자리에서 (구 1개) — 실측 평균 2.04
- 제거 수단: 휴식 '지우기' · 상점 제거 2회(50·80) · 이벤트 3종
- 죽은 코드 `takeReward`·`gainRelic` 삭제, `R.items.length<3` → `ITEM_CAP`

## 3. 드래그 전용 발동
- **클릭으로는 카드가 나가지 않는다.** `div.onclick`의 발동 경로를 걷어내고 `zoomCard()`(전체 화면 확대 보기)로 대체
- 선택형(`choice`) 카드도 끌 수 있게 되고, **놓은 뒤** 선택지가 뜬다
- `nearFoe(x,y)` — 적 아이콘을 정확히 맞히지 못해도 적 줄 근처면 가장 가까운 적을 잡는다(드래그 중 강조도 같은 규칙)
- 첫 전투 1턴에 `dragTip()` 안내 1회 (localStorage 금지 → 세션 변수)

## 4. 조력자
- **말풍선 = 흰 바탕 검은 글자 15.5px**, 폭 300px, 표시 4.2초
- **전투 밖 대사도 머리 위로.** 구 `allyQuote()`(화면 상단 인라인 `.allyquote`/`#mapTalk`)를 폐기하고 `railBark(kind)`로 통일 — "맵에서 대사가 맨 위에 뜬다"의 진짜 원인이 이것이었다
- `.down` 판정을 상수(`top<180`) → **붙인 뒤 실제 rect**로. 화면 위아래 양쪽을 검사한다
- `hideRunScreens()`에서 `barkClear()` — 전투에서 쌓인 대사가 맵으로 흘러나오던 것 차단
- 멈블 볼륨 2.2배(`VOICE_TONE.vol` 0.066~0.085 → 0.145~0.187)

## 5. 아이템·유물 UI
- 아이콘 크기가 `icon(cat,id,size)` **인자에 하드코딩**돼 CSS로는 못 키운다 → 렌더 호출부를 함께 수정
- 전투 아이템 20 → **30px**(버튼 실측 54×46) · 유물 18 → **26px**(칩 46×40) · 맵 HUD 17 → 22px · 트레이 폭 280 → 380 + 모바일 미디어 쿼리

## 6. 합성
- **강화 창에서 합성 카드 제외** (`locked = upMode && (en.up || en.id2)`), `showUpPreview`가 `id2/up2`를 버리던 것도 수정
- **칩을 우상단으로** — 좌상단은 계절 메달 자리였다. 칩이 있으면 `.seastag`가 한 칸 내려간다
- 병합 확장: `dmgMulti`(피해·관통이 같을 때 횟수 합산) 추가 · 약화/취약 `maxN` → **`sumN`(상한 4턴)** · 합칠 수 없는 효과는 문구를 `×2`로 접는다

## 7. 이벤트·상점·유물
- 이벤트 **7 → 18종**(공통 12 · 막별 2씩). `R.evSeen`으로 한 여정 중복 금지, 막 전용 필터
- 일반 유물 **6 → 16종**(황도광 프리즘·별먼지 주머니·은빛 각도기·운석 목걸이·관측 일지·흑요석 거울·천구의 사슬·항성 나이테·부싯돌·은하수 지도). 획득은 `takeRelic()`으로 일원화(얻는 즉시 효과가 있는 유물 대응)
- 상점: 카드 4장 · 아이템 3종 · **강화 서비스** · 제거 2회 · 막별 시세(1.0/1.15/1.3) · 은하수 지도 20% 할인
- 가독성: 공통 `.panelcard`(아이콘 + 이름 + 설명 + 값 배지) · 살 수 없으면 `.dis` · 이벤트 아트 150 → 260px · 본문 15.5px

## 검증 (런타임 에러 0)
```
v0.25 | 별 111 · 별자리 26 · rank/문자순서 위반 0 · 죽은 id 참조 0 · si 오류 0
장수 분포: 2장×1 · 4장×16 · 5장×9 = 111
등급: 황소 2만회 추첨 rank 1/2/3/4/5 = 5.7/11.6/20.0/28.2/34.6% (가중치와 일치)
수급: 시작 덱 9장 · 3택 중 보유 별자리 평균 2.04개 · 담기 토글 후 덱 9→11 정확
드래그: 클릭 시 손패·물질 불변 + 확대창 열림 · 적 드롭 → HP 26→10 · 게임판 드롭 발동 · 손패 드롭 취소 · 잔여 스타일 0
조력자: 화면 상단 인라인 대사 0개 · 말풍선 흰 바탕(255,253,245)/검은 글자/15.5px · 3인에서 뷰포트 안 · 본인 위
아이템: 아이콘 30px·26px · 버튼 54×46 · 칩 46×40
합성: 다단히트 {v:7,n:2}×2 → {v:7,n:4} · 약화 1+1 → 2턴 · 누적류 "피해 5+누적×2" · 강화 픽커 잠금 2장 · 칩↔계절메달 안 겹침
이벤트: 18종 · 40회 추첨에서 서로 다른 14종(1막 풀 크기와 일치) · 막 위반 0
상점: 카드 4 · 아이템 3 · 유물 1 · 패널 6개 · 강화 서비스 동작
```

## 이번에 밟은 함정
1. **페이로드 재배치가 밸런스를 지켰다** — 111장을 예산에 맞춰 전부 재산정하는 대신 별자리 안에서 효과를 옮기기만 했더니 보스 승률이 ±3%p 안에 머물렀다. 대규모 데이터 개편에서 **총량 보존**을 먼저 설계하면 재측정 부담이 사라진다.
2. **테스트 창이 짧으면 손패가 뷰포트 밖**이라 `elementFromPoint`가 `null`을 돌려주고 "손패 위 드롭 = 취소"가 오작동하는 것처럼 보인다. 드래그 검증은 **900px 이상 높이**에서 할 것.
3. `background: linear-gradient(...)`는 `getComputedStyle().backgroundColor`가 `transparent`로 나온다 — 흰 배경 검증은 `backgroundImage`를 봐야 한다.

---

# v0.26 — polish: 막혀 있던 조작을 뚫다 (2026-08-10)

`전투프로토타입_v0.26.html` (약 1.25MB). Impeccable 스킬의 `critique`(19/40) → `polish` 결과.

## P0 — 기능이 막혀 있던 두 곳

### 1. 조력자 3인이면 "턴 종료" 버튼이 눌리지 않았다
`#allyBox`는 `pointer-events:none`인데 `.allyone`이 툴팁 때문에 `auto`로 되돌려, 156px 폭 실루엣이 버튼을 덮었다.
계측: **3인 0/49 · 1인 45/49** (1366×660, 843×595 동일). 전역 `keydown`이 없어 대체 경로도 없었다 —
**3막에서 흔한 노트북 해상도로는 턴을 끝낼 수 없었다.**
- `.allyone`을 `pointer-events:none`으로 되돌리고 **호버 타깃을 이름표(`.aname`)로** 옮겼다 (`.aname:hover ~ .abody`)
- `.allybark`·`.alabel`도 `pointer-events:none`
- `#endTurn{position:relative;z-index:26}` (레일 24보다 위)
- `#deskPanel{padding-right:var(--rail)}` — 레일 폭을 실제 레이아웃으로 예약
→ **45/49**(남은 4는 원형 버튼 모서리 바깥, 정상)

> **왜 내 v0.25 검증이 놓쳤나**: 전투 자동 진행을 `endTurn()` **함수 직접 호출**로 돌렸다. 버튼을 클릭하지 않으면 가림은 절대 드러나지 않는다. 이후 검증은 `elementFromPoint`로 **실제 클릭 지점**을 찍는다.

### 2. 세로가 좁으면 손패가 잘렸다
843×595에서 손패 **111px 절단**, 조력자 선택 화면은 제목이 y −362로 **도달 불가**.
- `#menu,#deckSel`에 `overflow-y:auto` + `justify-content:flex-start`
- **`fitBoard()` 신설** — 창 높이에 맞춰 카드를 자르는 대신 줄인다

`fitBoard()`가 밟은 함정 네 개(전부 실측으로 잡음):
1. **CSS는 길이를 나눠 무단위 비율을 못 만든다.** `calc((100vh - 300px)/262)`는 무효라 `--card-scale`이 통째로 죽고 손패 높이가 14px로 붕괴했다 → JS가 계산해 넣는다.
2. **고정 공식이 안 맞는다.** 비-손패 크롬이 화면마다 달라 → `--card-scale`을 1로 두고 **재서** 맞춘다.
3. **`document.scrollHeight`는 하단 여백 63px까지 센다** → 900px 창에서 멀쩡한데도 카드를 0.775로 줄였다. 기준을 **`#deskPanel` 하단**으로 교체.
4. **성좌판을 먼저 접으면 적이 58px 잘린다** → 우선순위를 뒤집어 **카드를 먼저 줄이고**(확대 보기가 있어 손실이 적다), 그래도 넘칠 때만 성좌판을 접되 **적 하단을 바닥으로** 삼는다.

결과 — 1280×900 배율 1.0(152×252) · 1366×660 0.74(112×186) · 843×595 0.62 · **전부 적 잘림 0 · 손패 잘림 0**

### 3. 모바일 손패가 화면 밖으로 78px 나갔다
`#hand`는 `#bottomRow` **그리드 안이라 폭이 20px**까지 줄어든다 — `calc(50% + …)`가 화면 중앙이 아니었다.
부채꼴 중심을 **뷰포트 기준**으로 잡고(`_hx`), 간격 계산도 `--card-scale`을 반영하게 고쳤다.
→ 375px에서 **5·8·10장 모두 이탈 0px**

## P1
- **키보드 경로 신설**: 숫자키 1~9 카드 내기 · Space/E 턴 종료 · Esc 취소·확대 닫기. `.hslot`에 `tabindex`+`aria-label`, `.enemy`에 `aria-label`, **`:focus-visible` 링**(파일 전체에 `:focus` 정의가 0개였다)
- **피드 잘림**: `.ftxt` `nowrap` 112px → `-webkit-line-clamp:3` 줄바꿈, 폭 174→210px. 계절 배지와 성좌 알약의 140×27px 겹침 해소
- **`.ccond` 8.5px → 12px** (성좌 완성 조건은 카드에서 가장 중요한 줄이다)

## 작은 결함들
- 사망 화면 버튼 두 개가 **둘 다 "처음으로"**였다 → `다시 떠난다` / `처음으로`. 여정 기록(막·갈림길·성도·가루·유물 + **완성한 성좌 목록**)을 남긴다 — 저장이 없는 게임이라 이 화면이 곧 결말이다
- 만피에서 휴식이 헛돌았다 → 비활성 + `이미 온전하다`
- `여정 포기`가 확인 없이 즉시 실행됐다 → 한 번 경고 후 재클릭
- `내 성도`에 **별자리별 보유/필요 수 알약** 추가 (`큰곰 5/3성`) — 3성을 노릴지 판단할 유일한 숫자였다
- 성막 0일 때 상시 `0` 칩 → 흐리게 물러남
- HUD 버전 라벨 v0.24, 조력자 선택 "주극 성도 12장"(실제 9장) 문구 교정
- `.enemy.targetable`의 `#ff8a6a`가 토큰에 없고 `.drophover`는 금색이었다 → 같은 뜻이므로 금색으로 통일

## 검증 (런타임 에러 0)
```
탐지기 3건 — 전부 critique에서 "브리프가 이김"/"오탐"으로 판정한 항목 (피드 칩 좌측 띠 = 이벤트 종류의 유일한 구분 채널 ·
  HP바 width = overflow:hidden 단일 자식 · 대상 지정 발광 = 밤하늘 게임의 행동 유도)
전 화면 순회(맵·상점·휴식·대장간·보상·이벤트·보스보상·2막·3막) 오류 0 · 전투 완주 → 보상 화면
손패 tabindex 5 · aria-label 7 · focus-visible 규칙 3 · 숫자키·Esc 동작
모바일 375×812: 손패 5/8/10장 이탈 0px · 트레이↔레일 겹침 없음
```


---

# v0.27 — 드래그는 '낸다'까지, 대상은 지정 화면에서 (2026-08-10)

`전투프로토타입_v0.27.html`. 사용자 지시: "카드 낼 때 드래그로 낸 후 지정 화면으로 넘어가게. 드래그로 바로 대상 지정하지 말고."

- **드롭 = 카드를 낸다**(대상 카드도 판 아무 곳). 곧장 적을 찍던 `nearFoe` 즉발 경로 폐지 → `S.mode='target'`
- **지정 화면**: 판 테두리가 금색으로 켜지고(`body.aimmode`), 겨누는 카드만 밝게 남으며(나머지 손패는 `brightness(.5)`), 적마다 **`겨눔 1·2·3` 배지**가 붙어 맥동한다
- 취소: **Esc · 배경 클릭** → 카드가 손으로 돌아온다(비용도 소모되지 않는다)
- **키보드도 같은 흐름**: 숫자키로 카드를 내면 지정 화면, 거기서 숫자키가 적을 고른다. 배지 번호와 키가 일치한다
- 드래그 중 개별 적 강조는 폐지 — 이제 게임판 전체가 '놓는 곳'이라 `dragfree` 글로우만 남는다
- 대상이 없는 카드는 종전대로 드롭 즉시 발동

## 검증 (런타임 에러 0)
```
판에 드롭 → mode target · 손패 그대로 · 적 HP 불변 · 카드 강조 1 · 판 어둡기 on
적 클릭 → 발동(41→25) · 숫자키 흐름(카드 → 지정 → 적) 동작 · 배지 ["1 겨눔","2 겨눔","3 겨눔"]
Esc / 배경 클릭 300ms 뒤 → 취소, 카드 손으로 복귀
대상 없는 카드 → 즉발 유지 · 전투 완주 → 보상 화면 · 전 화면 순회 오류 0
```

## v0.28 — 적이 하나면 자동 지정 (2026-08-10)
`전투프로토타입_v0.28.html`. 사용자 지시 "하나면 자동지정해야지".
`alive().length===1`이면 지정 화면을 건너뛰고 곧장 발동한다 — 드래그 경로와 키보드 경로 둘 다.
살아 있는 적 기준이라 **셋 중 둘이 죽어 하나만 남아도 자동**이다.
검증: 적 1명 드롭 → 즉발(26→20) · 적 3명 → 지정 화면(배지 3) · 3명 중 1명 생존 → 즉발(10→2) ·
키보드 자동 지정(41→34) · 전투 완주 → 보상 화면 · 오류 0

---

# v0.29 — z-계층 정리: 갇힌 턴 종료 버튼 (2026-08-11, 원격 세션)

`starry-night-v0.29.html`. 사용자 승인 4건(브리핑·리뷰 후속): 문서 정정 · 스택 문맥 수정 · z-index 토큰화 · 시뮬 13행 정렬. **게임 규칙·데이터 변경 없음** — v0.28과 `dump_sim_data.js` 덤프가 바이트 단위로 동일하다.

## 1. 좁은 창(≤414px) + 마우스에서 턴 종료 버튼이 눌리지 않던 문제

- 증상: 390×844(마우스)에서 손패 3·5번 카드에 커서를 올리면 호버 확대 카드(233×404px)가 턴 종료 버튼을 완전히 덮었다. 원 안 표본 45점 **0/45 관통**, 실제 클릭도 실패.
- 원인: `#playerBar{position:relative;z-index:2}`가 **스택 문맥**을 만들어 `#endTurn{z-index:26}`이 그 안에 갇히고, 형제 문맥 `#hand{z-index:2}`(호버 슬롯 99)가 DOM 순서로 이겼다 — v0.26 조력자 레일 사건과 같은 계열.
- 수정: `#playerBar`에서 z-index 제거 → `#endTurn`이 `#deskPanel` 문맥에 직접 참여해 26 > 2로 이긴다.
- **실제 터치 기기는 원래 안전했다**: 호버 확대가 `@media (hover:hover)` 안에 있다. 영향 범위는 좁게 줄인 데스크톱 창·폴더블·마우스 붙은 태블릿.

## 2. 전역 z-index 토큰화 (재발 방지)

`:root`에 `--z-*` 토큰 21종(`--z-bg` 0 ~ `--z-drag` 300)을 선언하고 **전역 계층 33곳**을 전부 토큰으로 치환. 카드·칩 내부의 1~9는 각자 스택 문맥 안 지역 값이라 그대로 뒀다. 수치는 전부 불변 — 계산값 비교로 `#playerBar`(2→auto) 외 차이 0을 확인.

## 3. 시뮬레이터 13행 정렬 (`sim_battle_v2.py` · `sim_tune.py`)

- `sim_battle_v2.sim_act`가 마지막 남은 **9행 모델**이었다(sim_tune은 v0.21부터 13행) → 빌드 `genMap`과 같은 13행·확률(전투 .556 / 중간보스 .083 / 휴식 .139 / 이벤트 .111 / 상점 .083 / 대장간 .028)·티어(0~2/3~8/9~)·대장간 합성·3막·휴식 30·막 사이 22로 정렬
- 보스 전 고정 휴식도 빌드값(30)으로 — sim_tune 포함. **1막 클리어율 3.9% → 5.9%가 전부 이 회복 정렬 효과**임을 A/B로 귀속
- 커밋된 코드가 저장소에 없는 `sim_data_v024.json`을 읽던 것을 v025로 교정(새 클론에서 즉시 깨졌다) · `--validate`의 관통 검산 카드도 v0.25에서 잘린 nunki → 웨젠으로 교체
- 수치는 밸런스로그 18차

## 검증 (런타임 에러 0)

```
v0.29 | 별 111 · 별자리 26 · 적 34 · 도감 111장 · pageerror 0 · console 경고 0
z-index 계산값: 토큰화 전후 차이 = #playerBar(2→auto) 단 1건 (의도된 수정)
턴 종료 관통(원 안 45점): 1366×900·768·660 / 843×595 / 390×844 / 375×812 / 414×896 / 360×640 전부 45/45
  + 손패 1~5번 각각 호버한 상태에서도 전부 OK (v0.28은 390×844에서 3·5번 호버 시 0/45)
fitBoard: 1366×768 배율 1.0 / 1366×660 0.744 / 843×595 0.62 — 손패·적 잘림 0 (v0.28과 동일)
드래그: 적 1 자동 지정 즉발 · 손패 5→4 · 터치 에뮬 탭→확대→Esc→턴 종료 탭 성공
시뮬: --validate 6항목 전부 기대값 일치
```

## 알려진 한계 (v0.29에서 손대지 않음 — 별도 결정 필요)

- **375~390px에서 적 2마리 조우 시 손패가 233px 잘린다** — 적 줄이 줄바꿈되며 판이 길어지는 문제로 **v0.28에도 동일**(이번 수정과 무관, 실측으로 확인). fitBoard가 적 2마리 세로 배치를 반영하지 못한다
- `prefers-reduced-motion` 대응 0건 (@keyframes 30개) — critique 지적 중 유일하게 남은 항목
- 색 토큰화는 미착수 (하드코딩 hex 93종 · rgba 134종)
- `sim_bosses.py`를 저장소 스냅샷으로 재실행하면 보스 승률 절대값이 sim_final_report의 표와 다르다 — 리포트 표는 원본 PC의 로컬 상태(구 덤프 등)로 잰 것. 이후 기준선은 18차 수치

---

# v0.30 — 로그 대개편·기록 패널 · 맵 가독성 · 375px · 드래그 선택 방지 · 휴식 합성 폐지 (2026-08-11, 원격 세션)

`starry-night-v0.30.html`. 사용자 피드백 5건. 게임 데이터(STARS 등) 불변, 휴식 합성 폐지만 밸런스 변경(19차).

## 1. 드래그 중 텍스트·이미지가 딸려오던 문제
`user-select` 계열 선언이 파일에 하나도 없었고 pointerdown에 `preventDefault`도 없었다 → `body{user-select:none}` + `img{-webkit-user-drag:none}` + 손패 pointerdown에 `preventDefault()`. 클릭 확대(`zoomCard`)는 영향 없음(실측).

## 2. 375px에서 적 2마리 조우 시 손패 233px 잘림 (v0.28부터)
원인 셋을 전부 수정: ① `fitBoard()`가 `render()` 안에서 적/손패 DOM 재구축 **이전**에 호출돼 낡은 레이아웃을 측정 → 함수 끝으로 이동 ② 축소 규칙 `.many`가 3마리부터라 2마리는 좁은 화면에서 무조건 줄바꿈 → `alive>=2 && innerWidth<=600`도 축소 + `@media(max-width:600px)` 폭 축소(bigfoe 190·일반 150·minion 118 — 375px 콘텐츠 폭 323px에 어떤 2마리 조합도 한 줄) ③ 그래도 넘치면(3마리+) **하늘에 내부 스크롤** — 손패가 화면 밖으로 밀리는 것보다 낫다.

## 3. 맵 가독성 (사용자: "선이 안 보이고 캡션이 거슬린다")
- **선 3상태**: 지나온 길(금 실선) · **개척선**(현 위치→갈 수 있는 노드, 굵은 금선 + dash-offset 흐름 애니메이션 — 구버전엔 이 상태 자체가 없어 다음 길이 α.14 파선이었다) · 나머지(α .14→.30)
- **현 위치 = 금색 이중 링**(`::after`) — 구 민트 글로우는 휴식 노드 색(#7fe8b0)과 동일해 구별 불가였다. 못 가는 먼 노드는 opacity .55
- **캡션 전부 제거 + 호버·포커스 전용**(어두운 알약 12px) — "전투" 20회 반복 소음 제거. 열린 노드에 tabindex+Enter 경로 추가

## 4. 휴식 '합성' 폐지 (사용자 지시 — 밸런스로그 19차)
휴식 = 휴식/강화/지우기/성도 보기. 합성은 대장간 노드(맵당 1)와 2막 이벤트에서만. 휴식→합성 진입 후 취소하면 맵으로 가버리던 함정 구조도 함께 소멸. 시뮬(sim_tune 휴식 분기·sim_bosses 가정 덱)도 정렬 — **낙폭이 크다**(1막 5.9→2.1%, 암흑 물질 22.8→0.8%). 후속 조정은 사용자 결정 대기(19차의 4개 선택지).

## 5. 로그 대개편 + 전투 기록 패널 (탐사에서 17건 문제 확인 → 전부 해소)
- **파이프라인 단일화**: `log(msg,{kind,icon:{type,id},life,quiet})` — 아이콘 raw HTML 계약 폐지(`feedIcon` 한 곳이 렌더). 이력은 `S.history` 배열
- **조력자**: 대사는 말풍선만(`pumpBark`의 무조건 `log()` 삭제 — "조력자 말이 로그창에 팝업" 해소). 피드에는 **능력 발동만**, 7곳 전부 `PORTRAIT_IMG`(v0.22 webp) 초상으로 통일 — "바뀐 얼굴이 로그에 적용 안 됨"의 원인이던 구 SVG 초상 `ART.portrait`(4.7KB)은 도달 불가 폴백까지 삭제
- **칩 정리**: `f-card` CSS 신설(무색이던 카드 칩) · 플레이어 피격 = `f-hit` 강조(굵은 적색 테두리) · 보스 등장/대사 = `f-boss` 10초 · 성좌 완성 10초 · 아이템 칩 아이콘 규칙 통일 · 턴 구분선은 칩이 아니라 얇은 눈금(`.fdiv`) + 턴 시작 맨 앞으로 이동(기록이 시간순으로 읽힘)
- **수명·정리**: 축출 칩 타이머 해제(누수) · `hideRunScreens()`에서 `feedClear()` — 피드·기록 패널·드래그 안내가 화면 전환 후 남지 않는다(구버전은 z-index가 우연히 가려 주는 구조였다)
- **전투 기록 패널 신설**: 피드 위 `☰ 기록` → 이번 전투 전체 이력(아이콘·색·턴 구분선) 오버레이, Esc/✕/배경 클릭 닫기. 구 죽은 `#log`(innerHTML 무한 누적, 읽는 곳 0)는 삭제

## 검증 (런타임 에러 0)
```
v0.30 | 도감 111 · pageerror 0 · JS 문법 OK · 휴식 버튼 4종(합성 없음) · openForge 호출 = 대장간+이벤트 2곳
드래그: 드래그 중 getSelection()='' · 클릭 확대 정상 · 카드 발동 정상
375×812: 적 1·2마리 한 줄·잘림 0 / 3~5마리(복제 스트레스) 줄바꿈+하늘 스크롤·잘림 0 · 턴 종료 관통 45/45 전 해상도
맵: 시작 개척선 0(의도)·승리 후 개척선 2+금 링 1 · 캡션 호버 표시 · dim 61선 α.30
로그: 잡담 칩 0 · 능력 칩 webp 초상 · f-hit/f-boss/눈금 동작 · 기록 패널 시간순 · Esc 닫기 · 맵 전환 후 칩 0·안내 0
시뮬: --validate 6항목 통과
```

---

# v0.31 — 조력자가 말하고 숨쉰다: 입 벙긋 + 대기 들썩임 (2026-08-11, 원격 세션)

`starry-night-v0.31.html` (약 1.40MB — 입엶 프레임 7장 +202KB). 사용자 제공 `조력자2.png`(입엶 픽셀 시트) 반영.

## 1. 입 벙긋 (`mouthTalk`)
- 말풍선이 떠 있는 동안(3.4초) 해당 조력자의 스프라이트가 기본(`PORTRAIT_IMG`)↔입엶(`PORTRAIT_OPEN`)을 **135ms 간격**으로 교차. 끝나면 기본 프레임 복원, `barkClear()`(화면 전환)에서도 안전 정지
- `PORTRAIT_OPEN`이 비어 있으면 조용히 비활성 — 그림 없는 빌드에서도 동작 불변

## 2. 대기 들썩임 (`idlebob`)
- 스프라이트에 `steps(1)` 스냅 모션(1.1s, translateY 0→−3px) — 픽셀 게임식 뚝뚝 끊김. 2·3인째는 `animation-delay`로 박자를 어긋냈고, 능력 발광(`aglow2`) 중에도 유지

## 3. `embed_assets.py` 짝 모드
- `조력자.png`(입다뭄, 기본) + `조력자2.png`(입엶)를 **짝지어** 추출: 입엶 시트는 인물이 얼굴·옷·소품 조각으로 떨어져 검출되므로(코페르니쿠스는 혼천의만 잡혔었다) **작은 성분(0.05%~)까지 모아 기본 시트 인물 중심에 귀속**시켜 합친다
- 두 시트 마스크의 **합집합 상자 하나로 둘 다 크롭** → 프레임 크기·배율·anchor 일치(272×340 등 동일 확인) — 입 벙긋 때 몸이 튀지 않는다
- 입엶 시트가 없으면 구 단일 시트 모드로 폴백

## 검증 (런타임 에러 0)
```
PORTRAIT_IMG 7 · PORTRAIT_OPEN 7 · 짝별 프레임 치수 동일 (갈릴레오 272×340 = 272×340)
idlebob: 계산값 animation=idlebob 1.1s steps(1) · 3인 딜레이 0/.37/.74s
mouthTalk: 교차 실측(open↔base) · 종료 후 기본 복원 · 타이머 정리 · PORTRAIT_OPEN 없으면 무동작
인게임 캡처: 두 프레임 같은 자리 (헤드 위치 일치)
```

## 메모
- 기본(회화풍)과 입엶(픽셀풍)의 **그림체가 다르다** — 사용자 확인 후 그대로 진행("그냥 하면돼"). 기본 시트도 픽셀풍으로 교체되면 `조력자.png`만 갈아끼우고 `python embed_assets.py <빌드>` 재실행이면 끝

---

# v0.32 — 맵 10행 · 들썩임 완화 · 개발용 막 점프 (2026-08-11, 원격 세션)

`starry-night-v0.32.html`. 사용자 지시 4건 중 3건 (조력자 기본 얼굴 교체는 새 조력자.png 도착 대기).

## 1. 맵 13행 → 10행 (관문 15 → 12, 전체 45 → 36)
사용자 "스테이지가 너무 많다" → 선택지(9/10/11) 중 **10행** 확정. 특수 노드 비례 조정(상점2·휴식4·이벤트3·대장간1), 중간보스 구간 [2,4][5,7][8,9], 티어 0~2/3~6/7~. 시뮬 3종 정렬 + 재측정은 밸런스로그 20차.

## 2. 대기 들썩임 완화
v0.31의 `steps(1)` 스냅(1.1s)이 "너무 심하다" → **2.8s ease-in-out 호흡**(천천히 올라갔다 내려온다)으로. 인원별 박자 어긋냄(0/.95/1.9s)·발광 중 유지 그대로.

## 3. 개발용 막 점프 (시작 화면)
`개발용 — 2막부터 / 3막부터` 버튼. `devStart(n)` = 정상 `startRun` 후 막마다 `devGrow()`(별자리 2곳 집중 +4장씩 · 강화 3 · 대장간 합성 1쌍 · 가루 +120) + 보스 유물 1 + 조력자 합류를 실제 막 전환 함수(`startAct2/3`)로 진행 — **게임 경로를 그대로 타므로** 상태 정합이 보장된다.

## 검증 (런타임 에러 0)
```
맵: 10+2행 · 노드 {battle 17(중간보스 3 전환 후 14+3)·shop 2·rest 4+1·forge 1·event 3·boss 1}
3막 점프: act=3 · 덱 23장(합성 2·강화 6) · 조력자 3인 · 보스 유물 2 · 가루 240 · 3막 전투 정상
2막 점프: act=2 · 덱 16장 · 조력자 2인 · 유물 1
들썩임: idlebob 2.8s ease-in-out (계산값 확인)
시뮬: --validate 통과 · 수치는 밸런스로그 20차
```

## v0.32 추기 — 조력자 기본 얼굴 픽셀화 완료 (같은 날)

사용자가 새 `조력자.png`(입다뭄 픽셀, 1254², 검정 배경)를 푸시 → 기본·입엶 모두 픽셀풍으로 통일. `embed_assets.py` 짝 모드 3차 개정:
- **flood tol 10 → 3**: 검정 배경에서 tol 10은 어두운 머리칼·외투를 배경으로 먹었다 (케플러 몸통 소실). tol 3 = 기본 시트가 정확히 7덩어리
- 잔조각 귀속에 **거리 제한**(인물 상자 40% 반경) — 떠도는 부스러기가 캔버스를 부풀리면 contain 스케일로 인물이 작아진다
- 캔버스가 다른 두 시트(1254² vs 1536×1024) 대응: **키 정규화 + 바닥 중앙 정렬**로 프레임 일치. 치수 검증 7/7 (220~330×340)
- 덤: 사용자 로컬에서 `게임기획서_v0.2.md`·`시스템설계_수급과성장.md` 복원 푸시됨 · 원클릭 `동기화.bat` 도입

---

# v0.34 — 울트라 리뷰 후속: 버그 10건 + 마찰 2건 수정 (2026-08-12, 원격 세션)

`starry-night-v0.34.html`. 전 파일 정밀 리뷰(10건)와 자동 플레이 봇(1막 전 구간 3회 완주)에서 나온 문제 전부 수정.

## 게임플레이 버그
1. **전투 최대 HP가 70 하드코딩** → `R.maxhp` 반영. 항성 나이테(+8)를 얻으면 이제 전투에서도 78 상한 (구버전은 회복하면 오히려 −8 손해). UI `/70` 하드코딩 3곳(전투 statbox·맵 HUD·휴식)도 동적으로
2. **도트·가시가 분열·2페이즈를 우회** → `hit()` 안에만 있던 문턱 검사를 `foeThreshold()`로 분리해 중독·화상·가시 피해 후에도 호출. 검증: 중독으로 플레이아데스 50%를 통과시키면 분열 발동
3. **미라크 `alsoCon:'pegasus'` 누락** → 복구. 검증: 마르카브+미라크 한 턴 = 페가수스 완성
4. **조력자 능력 대사 쿨다운이 전투 간 잔존** → `startBattle`에서 `barkCd={}` 리셋
5. **조건부 효과 15장 카드 미표기** → `.cxtra` 줄 신설 (`※ 이번 전투에 오리온 성좌를 완성했다면 피해 +4`)
6. **상점 무한 루프 (보유 별자리 카드 종수 < 3)** → 60회 시도 후 전체 풀로 전환하는 가드
7. **'여명 직전' 이벤트 문구 ≠ 코드** → 문구를 실동작("절반까지 채운 뒤 +18 회복")에 맞춤

## 마찰 (자동 플레이에서 발견)
8. **휴식에 '떠난다' 신설** — 만피+최소 덱이면 원치 않는 강화를 강제당하는 함정이었다 (봇도 2회 갇힘)
9. **봉인 카드 조작 피드백** — 숫자키·드래그 시 좌우로 털리는 `sealshake` (구버전은 무반응)

## 위생
10. `#deckSel`이 런 시작 후 맵 뒤에 켜진 채 남던 것 정리 · 중복 `case 'drawMana'` 제거 · 읽는 곳 없는 `S.playedIds`/`S.compCount` 삭제 · genMap 주석 오기(중간보스 4회→3구간) 정정
11. **CLAUDE.md 스펙 정정** — 1막(맵 10행·관문 12·상점2/휴식4/이벤트3, M42 140/12·플레 95/10·히아데스 180/14)과 2막(M1 205/16·M13 155/12·블랙홀 330/23)이 v0.19 시절 수치로 남아 있었다
12. 시뮬 데이터 재덤프(미라크 반영) — `--validate` 통과, `sim_bosses` 20차와 동일(밸런스로그 21차)

## 검증
```
pageerror 0 · JS 문법 OK · 기능 검증 9항목 전부 통과(나이테 78 상한·도트 분열·미라크 완성·barkCd 리셋·
상점 가드 4장 종결·휴식 5버튼+떠난다 동작·extraTxt 렌더·sealShake·deckSel 정리)
자동 플레이 봇: 1막 전 구간 완주(보스 격파) · 턴 종료 관통 45/45 회귀 유지
```

---

# v0.35 — 조력자 말수 조절 · 순백 말풍선 · 칩 겹침 (2026-08-12, 원격 세션)

`starry-night-v0.35.html`. 사용자 피드백 4건 (기획 논의 4건은 별도 진행).

1. **말이 너무 많고 텍스트를 가림** → 턴 잡담 75%→30% · 받아치기 62%→40% · 3인 끼어들기 20/15% · 맵 72%→45% · 상점/휴식/대장간/이벤트 진입 대사 매번→50% · **말풍선 대기열 상한 2**(밀린 수다는 버림) · 표시 4.2s→3.4s
2. **말풍선 = 순백 바탕 + 검은 글자** (사용자 재지시) — 크림 그라디언트(#fffdf5→#efeade)를 #ffffff/#111로, 꼬리도 흰색
3. **맵 HUD 조력자 얼굴이 구 라인 아이콘** → `portraitEl` 새 초상으로 교체 ("로그창 얼굴 동기화 안 됨"의 실체 — 피드 칩은 v0.34부터 정상)
4. **쌍성·강화 칩이 카드 이름을 덮음** (카시오페이아 β·γ 등) → `.tbar.haschip{padding-right:46px}` — 제목이 칩을 피해 줄바꿈

검증: 말풍선 계산값 rgb(255,255,255)/rgb(17,17,17) · 합성/강화 카드 렌더 겹침 0 · HUD `.portimg` 존재 · 플레이 봇 1막 완주 · pageerror 0

---

# v0.36 — 황도 12궁 완성 · 하늘 지표 · 공명/잔광 · 밤하늘 이벤트 · 계절 확장 · 보스 기믹 3종 (2026-08-13, 원격 세션)

`starry-night-v0.36.html`. 사용자 기획 확정("황도 12궁 카드 다 만들어봐 + 봄철 대삼각형 ㄱㄱ + 낼 때마다 UI에 별이 빛나게 + 나머지 키워드·이벤트 턴·계절·보스 다해")의 전면 구현. 카드 데이터는 `카드목록_v1.3.md` 신설.

## 1. 황도 12궁 완성 — 별자리 +5 · 별 +25

- 신설 별자리: **양(가을)·천칭(봄)·염소(가을)·물병(가을)·물고기(봄)** — 기존 황소·쌍둥이·게·사자·처녀·전갈·궁수와 합쳐 황도 12궁 전원 등장
- 별 25종 추가(별자리당 5장, 그리스 문자 순위 규칙 준수) → **총 136종 · 31개 별자리**, 필요 수 분포 1×1·2×16·3×14
- 성좌판·연출·카드 별선 통일: `gen_const_lines.py`에 J2000 좌표 5종 추가(물고기 서쪽 별 RA −360° 시프트) → `const_lines.json` 31키 → `gen_fx_lines.py` 재주입, `CON_ORDER/CON_SFX/CON_IDN/CON_FIG`도 +5
- 아이덴티티: 양 = 최대 HP 적 강타 · 천칭 = 균형(피해+성막 동시) · 염소 = 성막→회복 전환 · 물병 = 물질+뽑기 · 물고기 = 뽑기+비용 감소

## 2. 신규 키워드 2종 — 공명·잔광 (키워드 사전 16종)

- **공명**: 이번 턴 그 별자리 별을 (필요 수+1)장째부터 효과 증폭 — `fv(f,d)`가 `S.turnCounts` 검사(자기 자신 제외)
- **잔광**: 이번 전투에서 성좌를 1회라도 완성했다면 효과 증폭 — `S.completed` 검사
- 수치 표시: 효과 발현 창에 `(공명 +X)`/`(잔광 +X)` 명시. 시뮬레이터에도 같은 `fv` 메서드 이식

## 3. 하늘 지표 (SKY_MARKS) — "낼 때마다 별이 빛나는" UI

- 봄철 대삼각형(아르크투루스·스피카·데네볼라 → 최대 HP +4) · 여름철 대삼각형(베가·데네브·알타이르 → 물질 +1/턴) · 가을 대사각형(마르카브·셰아트·알게니브·알페라츠 → 성막 +7 시작) · 길잡이(메라크·두베·폴라리스 → 턴당 첫 완성 시 뽑기 1)
- **런 누적**: 해당 별을 카드로 낼 때마다(`markPlay`, 합성 카드 id2 포함) 별 하나가 **점등 + 반짝 애니메이션**(`.mtwinkle`), 세트 완주 시 특전이 런 내내 적용 — 특전은 `startTurn(true)` **이후에** 적용(에너지/성막 리셋에 안 씻기게)
- 전투 화면 `#markRow` 위젯: 지표 4종 진행도 상시 표시

## 4. 밤하늘 이벤트 턴 (SKY_EVENTS 5종)

- 턴 시작 시 12% 확률(2턴째부터, 일반전 최대 2회·보스전 1회): 유성우(뽑기+2) · 오로라(성막+6) · 별똥별(무작위 적 8 피해) · 혜성 접근(물질+1) · 어두운 구름(제철 보정 정지 1턴)
- 상단 배너(`skyBanner`)로 연출, 로그 kind='turn' 기록

## 5. 계절 확장 3종

- **제철의 축복**: 제철 별자리 성좌 완성 시 추가 효과 — 봄 HP+4 · 여름 화상 3 · 가을 가시 2 · 겨울 성막+5
- **계절 조작**: 소모품 '시간의 모래시계'(45가루, 계절 1칸 앞으로) · 유물 '느린 하늘'(75가루, 계절이 2턴마다 진행)
- **계절 한정 콘텐츠**: 계절 이벤트 4종(꽃보라·반딧불 든 밤·별의 수확제·고요한 밤 — `mapSeason()` 일치 시만 등장) + 상점 **제철 특가**(제철 별자리 카드 1장 30% 할인 `.ssale` 배지)

## 6. 약보스 기믹 3종

- **히아데스 — 황소의 눈**: 성막 위로 공격을 막아낼 때마다 반격 6 (`pHit` 헬퍼 신설 — 성막 흡수·게임오버 판정 포함)
- **M13 — 인력**: 4턴마다 버림 더미 맨 위 카드를 흡수하고 HP 8 회복 (의도 표시 포함)
- **M31 — 조석 꼬리**: 위성 은하가 죽을 때마다 본체 공격력 +2 (누적)

## 검증

```
기능 검증 전부 통과: 공명 6→10 · 잔광 3→5 · 제철의 축복 화상 3 · 황소의 눈 반격 6 ·
인력(폴라리스 흡수 + HP 100→108) · 조석 +2 · 지표 위젯 4종/점등/반짝/완주 특전(물질4·성막7) ·
도감 136장/31그룹 · JS 문법 OK · pageerror 0 · 자동 플레이 봇 1막 완주
시뮬 정렬: fv 이식 + 보너스 2종(blockHeal·dmgRandBlock2) → 재덤프(별 136·별자리 31) →
--validate 통과 · sim_tune --final 1막 4.8% · sim_bosses 이전과 사실상 동일 (밸런스로그 22차)
```

보류(사용자 합의): 기존 황도 9개 별자리의 시그니처 페이로드 재배치는 밸런스 재측정과 함께 다음 버전에서.

---

# v0.37 — 계절 전면 개편 · 하늘 지표 패시브화 · 겨울 지표 2종 · 작은개자리 (2026-08-13, 원격 세션)

`starry-night-v0.37.html`. 사용자 지시 세 갈래: ① "계절은 턴이 아니라 **한 게임(전투)·활동당 한 계절씩**, 계절을 아예 바꾸는 보스 기믹도, 계절 효과는 크게" ② "겨울철 삼각형은 왜 없나" ③ "지표는 내서 발동이 아니라 **덱에 갖고 있는 것만으로** 켜지는 패시브로, 겨울철 대육각형도".

## 1. 계절 = 전투당 고정 (턴 순환 폐지)

- `enterNode`가 그 관문의 계절을 `cfg.season`으로 넘겨 **전투 내내 `S.fixedSeason` 고정** — 전투(활동)마다 `R.seasonStep`이 한 칸 전진
- 보정 확대: 제철 **+25% → +50%**(올림) · 역철 −25% 유지 — 적 공격도 동일. 제철의 축복도 상향(봄 HP6·여름 화상4·가을 가시3·겨울 성막8)
- 사계의 천구의 = "두 턴에 한 번" → **"두 전투에 한 번"**(`R._skyTick`) · 계절의 모래시계 = 고정 계절을 한 칸 밀기
- 단판 보스전 = 무작위 계절 고정, 튜토리얼 = 무계절. 문구 전면 정리(튜토리얼 ④·2막 안내·맵 필·계절 라벨)

## 2. 블랙홀 신기믹 — 계절 왜곡

- 구 '계절 역행'(`reverse`, 턴 순환 전제라 사문화) 폐지 → **`seasonWarp:3` — 3턴마다 계절을 한 칸 끌어당긴다** (배경도 함께 바뀜). "계절을 아예 바꾸는 보스" 지시 반영

## 3. 하늘 지표 = 보유 패시브 (SKY_MARKS 6종)

- **성도(덱)에 지정 별을 모두 모으면 켜진다** — 내지 않아도 됨(구 `markPlay` "내면 점등" 폐지). 별을 지우거나 세트가 깨지면 특전도 꺼진다
- `refreshMarks()`가 덱 기준으로 재판정(맵 복귀·상점 구매·전투 시작) — 완성 순간 금빛 배너 + 로그
- **겨울철 대삼각형**(베텔게우스·시리우스·프로키온) = 전투 시작 시 무작위 적 피해 10 · **겨울철 대육각형**(카펠라·폴룩스·프로키온·시리우스·리겔·알데바란) = 매 턴 첫 카드 비용 −1 · 위젯은 6칸(육각형 링 렌더 추가)

## 4. 작은개자리 신설 (별 138종 · 별자리 32개)

- 겨울 지표의 꼭짓점 **프로키온**이 게임에 없어서("겨울철 삼각형은 왜 안 넣었나"의 답) 1성 별자리로 신설: **프로키온 α**(피해 11+뽑기 1) · **고메이사 β**(피해 5+성막 3), 완성 보너스 = 무작위 적 피해 5
- J2000 좌표로 별선 생성(`gen_const_lines.py`) → 32종 재주입 · 도감·소리(bark 고음)·삽화(작은 개)·아이덴티티 추가 · 겨울 별자리 5→6

## 검증

```
별 138 · 별자리 32 · CONST_LINES/FX 32 — 전투 계절 고정(2턴 경과에도 불변) · 제철 10→15 · 역철 10→7 ·
지표 6종 위젯 · 덱 보유만으로 winter3/hex 완성(내지 않음) · 섬광 10(적 79→69) · costDown 적용 ·
세트 깨면 특전 꺼짐 · 블랙홀 4턴째 봄→여름 왜곡 · JS 문법 OK · pageerror 0 · 자동 플레이 봇 1막 완주
시뮬 정렬(전투당 계절·+50%·왜곡 모델) → --validate 통과 · sim_bosses: 블랙홀 43.8→38.8% ·
암흑 물질 0.2→2.5% (밸런스로그 23차)
```

---

# v0.38 — 조력자 4프레임 립싱크·깜빡임 · 성좌 판화 삽화 32종 (2026-08-13, 원격 세션)

`starry-night-v0.38.html`. 사용자 제공 아트 2벌의 전면 반영. **빌드가 1.47MB → 1.1MB로 오히려 가벼워졌다**(아래 인코딩 전략).

## 1. 조력자 4프레임 (조력자/<이름>-talk-sheet.png 7벌)

- 512²×4(기본·입엶·입활짝·눈감음)·투명 배경·같은 격자 정렬 — 구 시트 2벌+flood 누끼 파이프라인보다 단순해짐(`embed_talk_sheets.py` 신설, 공통 크롭 후 1/2 NEAREST·WebP 무손실)
- **3단 립싱크**: 말풍선 동안 엶→활짝→엶→다뭄 4박(135ms) 순환 (`PORTRAIT_WIDE` 신설)
- **대기 깜빡임**: 말하지 않을 때 평균 3.5초마다 140ms 눈감음 (`PORTRAIT_BLINK`, 전역 500ms 인터벌 — 개인별 무작위라 박자가 어긋난다)
- 초상 168×256 무손실 WebP 28장 = **14KB** (구 2프레임 photographic WebP 대비 대폭 절감)

## 2. 성좌 판화 삽화 32종 전체 (별자리 그림/별자리그림1·2.png)

- 사용자 제공 판화풍 시트 2장(각 라벨 포함)을 `embed_conart2.py`로 분할 — 행 밴드 수동 확정 + 열 자동 검출 + 칸 하단 라벨 제거(양자리 잔글자 이슈까지 처리)
- **작은개자리 포함 32종 전부** — 구 각인 17종 + 수제 라인아트 15종 체제 종료. 완성 연출·성좌판 스테이지·도감·별자리 3택 패널이 전부 판화로 통일
- **인코딩**: 검은 배경 판화라 누끼(알파) 대신 **불투명 WebP + CSS `mix-blend-mode:screen`** — 검정이 곧 투명이 되고 알파 평면이 빠져 1959KB → **545KB**
- 구 `CON_FIG` 라인아트는 폴백으로 유지

## 검증

```
CON_IMG 32 · 도감 .conimg 32 렌더 · 완성 연출에서 판화 상승(카시오페이아) 확인 ·
립싱크 프레임 3종 순환 · 깜빡임 6초에 4회(2인) · 레일/맵 HUD/로그 칩 새 초상 ·
JS 문법 OK · pageerror 0 · 자동 플레이 봇 1막 완주 · 빌드 1045→1108KB(삽화 32종 포함)
```

---

# v0.39 — 픽셀풍 전환: 갈무리 폰트 내장 · UI 픽셀 테마 (2026-08-13, 원격 세션)

`starry-night-v0.39.html`. 사용자 지시 "배경·맵·카드·텍스트 전부 픽셀풍으로 — 너무 AI 느낌이 강하다. 우선 텍스트·기호부터, 카드 사진은 나중에 보내줌".

## 1. 한글 픽셀 폰트 내장 (`embed_font.py` 신설)

- **갈무리(Galmuri11 + Bold, OFL)** — npm 패키지에서 확보, `폰트/`에 원본 보존
- 빌드에 실제 등장하는 글자만 서브셋(1093종) → WOFF2 base64 @font-face **약 56KB** — 오프라인 단일 파일 유지
- 본문(고딕 스택)·타이틀(Georgia 이탤릭)·성좌 라벨(세리프) 전부 갈무리로 교체. **픽셀 폰트는 이탤릭이 흐려져** 기울임 전면 제거(타이틀·lore·cxtra)
- ※ **새 한글 문구를 넣은 버전마다 `python embed_font.py <빌드>` 재실행** — 서브셋에 없는 글자는 시스템 폰트로 빠진다

## 2. UI 픽셀 테마 (style 끝 블록)

- 모서리 직각화(버튼·필·패널·카드 틀·말풍선·지표 위젯·초상 칩 등 `border-radius:0`)
- 버튼 하드 섀도(블러 0, 눌림 2px 이동) · 제목 자간 축소 · `image-rendering:pixelated`(조력자·초상)

## 3. 낡은 메뉴 문구 일소

- 메뉴·HUD·도감 헤더가 v0.36/"별 111종·별자리 26개"로 하드코딩돼 있었다 → **v0.39 / 138종·32개**로 정정, 단판 안내의 "195종" 삭제

## 검증

```
Galmuri11 로드 확인(document.fonts.check) · 메뉴/맵/전투/카드 스크린샷 검수 · JS 문법 OK ·
pageerror 0 · 자동 플레이 봇 1막 완주(1회 패배는 무작위 변동 — 에러 아님) · 빌드 1.17MB
```

보류(사용자 예고): 카드 일러스트 사진 → 도착하면 반영. 적 도트·판화 삽화·배경은 유지.

---

# v0.40 — 카드 픽셀 성도 · 누끼 판화 · 말풍선 직렬화 · 조력자 이펙트 (2026-08-13, 원격 세션)

`starry-night-v0.40.html`. 사용자 지시 4건.

## 1. 카드 별자리 그림 = 픽셀 성도 (레퍼런스 `Constellation Atlas.dc.html`)

- `starArtSVG` 전면 재작성: 100×100 좌표를 **4단위 격자(25×25칸)에 스냅** → 별선은 **브레젠험 픽셀 선**(2px 셀, 중복 제거) → 별은 **밝기별 십자 반짝 스프라이트**(이 카드의 별 = 흰 코어+십자 3칸+대각, 밝은 별 = 작은 십자, 나머지 = 점) + 별자리별 고정 시드 잔별 22개. `shape-rendering:crispEdges`
- 손패·도감·로그 칩이 같은 함수를 쓰므로 셋 다 픽셀 성도가 된다. 데이터는 기존 `CONST_LINES` 그대로 — 세 화면 별 인덱스 통일 유지

## 2. 성좌 판화 = 사용자 누끼 (plate1·2-cutout.png)

- v0.38의 "불투명 + screen 블렌드" 우회 폐기 → **진짜 알파** 사용 (`embed_conart2.py` 알파 모드 전환, 셀 마스크도 알파 기준)
- 알파 페더의 미세 계조가 WebP 알파 평면을 3배 불려(1959KB) **4단계 양자화**로 731KB에 안착. 빌드 1.36MB

## 3. 말풍선 직렬화 ("한 문장을 끝내기 전에 이야기한다")

- 원인: 말풍선 표시 3.4s인데 다음 말풍선이 1.25s 만에 떴다 → **pumpBark 간격 3.9s** (문장이 끝나야 다음 말) · 립싱크도 3.4s로 정합

## 4. 조력자 능력 발동 이펙트 ("너무 밋밋")

- 공통 3종 신설: **스프라이트 펄스**(`apop` — 1.12배 + 밝기 섬광, steps(2)) · **금빛 픽셀 파편 8개**(`astarp` — 방사형, steps(6)) · **능력 이름표**(`allytag` — "✦ 망원경" 등 7종, 금 바탕 픽셀 칩이 떠오르며 사라짐)
- 기존 개인 이펙트(갈릴레오 빔·브라헤 물결·허셜 혜성·궤도 고리·팽창 빛무리)는 그대로 얹힘

## 검증

```
카드 픽셀 성도 렌더(손패 6장 스크린샷 검수) · CON_IMG 32(누끼) · 갈릴레오 빔+이름표 발동 캡처 ·
JS 문법 OK · pageerror 0 · 자동 플레이 봇 1막 완주(패배 1회는 무작위 변동) · 빌드 1.36MB
```

