# Product

<!-- impeccable:product-schema 1 -->

> 작성 방식: 사용자가 "그냥 다 해달라"고 진행을 지시해, 대면 인터뷰 대신 **기존 프로젝트 문서**(`CLAUDE.md`, `아트스타일가이드.md`, `카드목록_v1.2.md`, `개발현황.md`, `밸런스로그.md`)와 빌드 코드에서 확정 사실을 옮겨 적었다.
> 문서에 근거가 없는 항목은 **(추론)** 으로 표시하거나 "미확정"으로 남겼다 — 채워 넣지 않았다.

## Platform

web

## Users

- **주 사용자 = 제작자 본인**(1인 개발). 만들면서 직접 플레이하고, 플레이 뒤 구체적인 UI·규칙 피드백을 내어 다음 버전을 지시하는 방식으로 개발이 굴러간다. 지금까지의 모든 버전(v0.5~v0.25)이 이 순환으로 만들어졌다.
- **최종 대상 플레이어층은 미확정.** 문서 어디에도 연령·플랫폼·유입 경로에 대한 확정이 없다. 로그라이크 덱빌더 장르 관습(Slay the Spire류)을 아는 플레이어를 전제한 설계로 보이지만, 이는 **(추론)** 이며 확정 전까지 마케팅·온보딩 결정의 근거로 쓰지 않는다.
- 사용 상황: 브라우저에서 HTML 파일 하나를 열어 한 자리에서 한 여정(45관문)을 플레이한다. **저장이 없으므로 세션이 곧 한 판이다.**

## Product Purpose

별지기가 개별 **"별" 카드**로 덱을 짜고, **같은 별자리의 별을 한 턴에 필요 수만큼** 내면 **성좌가 완성**되어 보너스가 터지는 턴제 로그라이크 덱빌딩 게임.

적은 "별자리에 속하지 못해 소외된 천체들" — 행성·위성·혜성·성단·성운·블랙홀·암흑 물질. 1막(별의 요람) → 2막(계절이 도는 하늘) → 3막(이름 없는 것들)의 45관문을 지나 여정을 완주하는 것이 성공이다.

성공의 기준: **한 턴 안에 별을 이어 붙여 성좌를 완성하는 순간의 쾌감**이 게임의 심장이다. 덱·보상·상점·합성 등 모든 주변 시스템은 "그 순간을 얼마나 자주, 얼마나 크게 만드느냐"로 평가된다.

## Positioning

**실제 천문 데이터가 게임 규칙 그 자체다.** 별자리 그림은 J2000 실측 좌표를 별자리 중심 입체 투영한 것이고(Stellarium modern 표준 별선), 카드 이름은 바이어 부호이며(`작은곰자리 α (북극성)`), **카드 등급이 곧 그 별의 실제 밝기 순위**다 — α가 별자리에서 가장 밝고 가장 강하다.

주변 제품이 그대로 베낄 수 없는 지점: 별 이름·밝기 순서·별자리 소속·계절(남중 시기)·주극성 여부가 전부 **장식이 아니라 게임 수치의 근거**로 쓰인다. 미자르-알코르가 한 장으로 2성을 채우는 것도, 북극성이 완성 필요 수를 1 줄이는 것도 실제 천문 사실에서 나온다.

## Operating Context

- **배포·실행**: 단일 HTML 파일을 브라우저로 연다. 빌드 파이프라인·서버·설치 없음. 버전마다 새 파일(`전투프로토타입_vX.Y.html`)로 남기고 기존 파일은 덮어쓰지 않는다.
- **에셋 주입 파이프라인** (원본 그림을 갈아끼우고 스크립트를 다시 돌리는 방식):
  - `gen_const_lines.py` → `const_lines.json` → `gen_fx_lines.py` (별자리 선 — 카드·성좌판·완성 연출 세 화면을 한 데이터로 통일)
  - `gen_pixel_art.py` (적 도트 스프라이트 34종, 절차 생성)
  - `embed_assets.py` (조력자 초상 7인 + 계절 배경 4장), `embed_conart.py` (성좌 각인 17장)
- **밸런스 검증 파이프라인**: `dump_sim_data.js`로 빌드에서 데이터를 떠내 → `sim_battle_v2.py`(규칙 재현 + 그리디 AI) → `sim_tune.py`(전 구간) / `sim_bosses.py`(조우 시점 덱 기준 보스 승률). **시뮬 절대 승률은 하한선**이며, 판정은 보스 승률로 한다.
- **문서 규율**: 작업 전 프로젝트 md 문서를 전부 읽는다. 문서에 정해진 설정을 임의 창작으로 대체하지 않는다. 결정 이력은 `밸런스로그.md`에 근거와 함께 남긴다.

## Capabilities and Constraints

**기술 제약 (변경 불가)**
- 단일 HTML 파일 — HTML·CSS·JS 전부 인라인. 외부 스크립트는 cdnjs만.
- **localStorage 사용 금지.** 따라서 저장·이어하기·설정 영속·튜토리얼 완료 기억이 전부 불가능하며, 세션 변수로만 처리한다.
- 모바일 터치 대응 유지.
- 데이터 주도: 수치·효과는 상단 `STARS`/`CONSTELLATIONS`/`ENEMIES`/`ACTS`/`CONST_LINES` 등 객체에만 두고 로직에 하드코딩하지 않는다.
- 파일에 500KB 넘는 base64 한 줄이 여럿 있다(성좌 각인·초상·배경·도트·별자리 좌표). 편집 도구가 이 줄을 통째로 읽으면 안 된다.

**확정 규칙 (요약 — 전문은 `CLAUDE.md`)**
- 성간 물질 3/턴 · 드로우 5 · HP 70
- 성좌 완성 = **한 턴에** 필요 수를 채워야 하고, 발동은 턴당 성좌마다 1회
- 성좌 필요 수: 1성×1 · 2성×16 · 3성×9
- 계절: 1막 없음 / 2·3막 순환. 제철 +25% · 역철 −25% · 주극 무보정. 적도 계절을 탄다
- 카드 등급 = 별자리 안 그리스 문자 순위(rank 1~5), 별자리당 2~5장, 총 111종
- 카드는 **끌어다 놓아야** 나간다(클릭은 확대 보기)
- 조력자는 막마다 1인씩 합류해 최대 3인
- 소모품 13종(한도 5) · 유물 22종 · 이벤트 18종

**용어 (고정)**
별지기 · 별 · 별자리 · 성좌(완성된 별자리) · 성간 물질(마나) · 성막(방어도) · 별빛 가루(재화) · 버리기 · 대장간 · 쌍성(합성 카드) · 조력자 · 관문 · 여명(3막 규칙)

**명시적 미확정** (`CLAUDE.md` "미확정 결정" 그대로 — 지어내지 말 것)
1. 이스터에그 퀘스트(대삼각형·대사각형)의 구체 형태
2. 카드별 강화 상세 수치표 (현재 임시 일괄 규칙)
3. 4막 이후 구상 · 3막 전용 별자리/카드 보강
4. 정식 환율표 개정
5. 필요 수가 2성에 몰린 뒤 별자리 개성을 무엇으로 차등할지
6. **최종 대상 플레이어층 · 배포 계획** (문서에 없음)

## Brand Commitments

- **게임명 = Starry Night** (확정). 구 가칭 "성좌 전쟁" 폐기.
- **UI 언어 = 한국어.** 카드·로그·조력자 대사 전부.
- **조력자는 실존 천문학자 위인 7인**: 갈릴레오 갈릴레이 · 튀코 브라헤 · 캐럴라인 허셜 · 요하네스 케플러 · 니콜라우스 코페르니쿠스 · 에드윈 허블 · 헨리에타 리비트. 기획서 설정이며 **임의 창작 캐릭터로 대체 금지**(과거 "꼬마곰 폴라"를 임의 창작했다가 폐기한 전례가 있다).
- **카드 이름 = "별자리 + 그리스 문자", 고유명이 있으면 괄호로**: `작은곰자리 α (북극성)`.
- **문체**: 서술은 담담하고 문어적이다. 로그·이벤트 문구가 짧은 산문으로 쓰인다("밤하늘이 은빛으로 갈라진다"). 감탄사·과장 광고 문구를 쓰지 않는다.
- **이모지 금지** (v0.14에서 전면 제거하고 수제 SVG 아이콘으로 대체). 데이터에 남은 `icon:'🔭'` 필드는 사문화된 잔재다.

## Evidence on Hand

- **실측 천문 데이터**: `const_lines.json` (J2000 좌표, 별자리 중심 입체 투영, 큰곰 전신 18성 Stellarium modern 표준)
- **사용자 제공 원본 그림**: `조력자.png`(위인 7인) · `배경.png`(계절 4장) · `성좌각인.png`(3행×[6·6·5]칸) · `성좌연출_v0.22.png` · `Constellation Effect.dc.html`(완성 연출 레퍼런스)
- **절차 생성 에셋**: 도트 천체 34종(`gen_pixel_art.py`)
- **밸런스 측정 기록**: `밸런스로그.md` 17차까지 · `sim_final_report.txt`(v0.25 보스 승률 9종 실측) · `sim_data_v025.json`
- **전 카드 명세**: `카드목록_v1.2.md` (111종 전량 + 등급표)
- **없는 것 — 지어내지 말 것**: 실제 플레이어 테스트 데이터, 리텐션·전환 수치, 스토어 등재 정보, 외부 리뷰, 사운드 에셋 파일(모든 소리는 웹오디오 합성이다), `게임기획서_v0.2.md`(문서에 언급되나 로컬 미보유)

## Product Principles

1. **천문 사실이 규칙의 근거다.** 수치·이름·등급·계절을 정할 때 실제 별의 사실을 먼저 본다. 게임 편의를 위해 사실을 비트는 결정은 근거를 남기고 한다.
2. **성좌를 잇는 순간이 심장이다.** 모든 시스템은 "그 순간이 더 자주·더 크게 오는가"로 평가된다. 덱이 커져 성좌를 못 잇게 되면 그 시스템은 실패한 것이다.
3. **사용자가 시킨 것만 한다.** 새 아이디어는 임의 적용하지 않고 제안으로 분리해 묻는다. 문서에 정해진 설정을 임의 창작으로 대체하지 않는다.
4. **결정에는 측정이 따른다.** 밸런스를 건드리면 시뮬로 재고, 근거와 함께 로그에 남긴다. 측정 전에는 완료로 보지 않는다.
5. **버전은 보존한다.** semver로 올려 새 파일로 만들고 기존 버전을 덮어쓰지 않는다. 되돌아갈 수 있어야 과감하게 바꿀 수 있다.

## Accessibility & Inclusion

- **확정**: 모바일 터치 대응 유지 (≤820px 반응형 분기 존재). 카드는 드래그로 내며 `touch-action:none`으로 스크롤을 막는다.
- **확정**: 소리 없이도 플레이 가능해야 한다 — 모든 정보가 화면에 표시되고, 소리는 `AU.on` 토글로 끌 수 있다.
- **미확정**: 색각 이상 대응. 현재 등급·계절·상태를 **색으로 구분**하는 곳이 여럿이다(등급 구슬 5색, 계절 메달, 제철/역철 태그의 초록/빨강). 별도 요구가 확정된 바 없으므로 여기 기록만 해 둔다 — 필요 판단은 사용자 몫이다.
- **미확정**: 준수해야 할 접근성 표준(WCAG 등급 등)이 정해진 바 없다.
