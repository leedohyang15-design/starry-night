# 아트 스타일 가이드 v1.2

> v1.2 개정 (2026-08-10, v0.25 빌드) — **등급색 · 칩 자리 · 흰 말풍선 · 공통 패널**
> - **등급 = 별자리 안에서의 그리스 문자 순위**(rank 1~5). 색이 곧 등급이다 —
>   1 α `#e8c56a` 금 · 2 β `#b07fe8` 보라 · 3 γ `#5db3d9` 하늘 · 4 δ `#7fe8b0` 초록 · 5 ε `#9aa4b8` 은회
>   카드 제목의 `.bay`(그리스 문자)와 우하단 `.gem`(등급 구슬)이 **같은 색**을 쓴다. 툴팁은 `gradeTip()` — `α — 작은곰자리의 으뜸별`
> - **칩은 우상단으로**(v0.24는 좌상단이라 계절 메달 `.medal`을 덮었다 — 사용자 지적). `.upchip`/`.fusechip`는 `top:3 right:3`,
>   전투 중에만 뜨는 `.seastag`는 칩이 있을 때만 `top:25`로 내려간다(`.upcard .seastag,.fusedcard .seastag`)
> - **조력자 말풍선 = 흰 종이**(사용자 지시). `#fffdf5→#efeade` 그라디언트 · 2px 남색 테두리 · 글자 `#14172a` **15.5px/600** · 폭 `min(300px,84vw)`
>   꼬리는 `border-right+bottom`(위에 뜰 때) / `border-left+top`(`.down`). 위/아래는 **붙인 뒤 실제 rect로 판정**한다 — 상수 임계값은 긴 대사에서 화면 밖으로 나갔다
> - **전투 밖 대사도 말풍선으로.** 화면 상단 인라인 대사(`.allyquote`/`#mapTalk`)는 폐기 — 맵·상점·휴식·대장간 전부 `railBark()`로 조력자 머리 위에 뜬다
> - **아이템·유물은 화면에서 가장 작았다** → 전투 아이템 아이콘 20 → **30px**(버튼 54×46), 유물 18 → **26px**(칩 46×40), 맵 HUD 17 → 22px.
>   크기가 `icon(cat,id,size)`의 **인자에 박혀 있어 CSS만으로는 못 키운다** — 렌더 호출부와 패딩을 함께 고쳐야 한다
> - **공통 패널 `.panelcard`**: 상점 물건 · 이벤트 선택지 · 상점 서비스가 같은 모양을 쓴다 — 아이콘 28px + 이름 15px + 설명 12.5px + **우측 값 배지**. 살 수 없으면 `.dis`(카드 진열도 `.shopcard.dis`로 흐려진다)
> - **보상 담기/버리기**: `.rewpick`은 고르지 않으면 `opacity .5 + grayscale`, 고르면 금색 외곽 발광 + 하단 `✦ 담는다` 알약
> - **카드 확대 보기 `#cardZoom`**: 클릭이 더 이상 카드를 내지 않으므로 그 자리를 대신한다 — 전체 화면 암막 + `scale(1.75)`

> v1.1 개정 (2026-08-10, v0.24 빌드) — **카드 제목 체계 · 카드 격의 시각화 · 조력자 누끼 · 맵 위계**
> - **카드 제목 = "별자리 + 그리스 문자"**, 고유 이름이 있으면 부제로 (사용자 확정). 제목칸 가용 폭이 113px뿐이라 한 줄에 넣으려면 쪼개야 한다 →
>   `.cnm`(별자리, 10.5px 반투명) + `.bay`(그리스 문자, **16px 금색 세리프**). 부제 `.csub`는 `고유명 · 타입`(예: `포말하우트 · 공격`)
>   데이터는 `STARS[id].by`(그리스 문자) + `name`(고유명, 없으면 필드 자체를 뺀다). 표시는 `cardName()` / 좁은 칸은 `cardShort()`
> - **카드의 격 = 프레임 재질, 상황 = 발광** (역할 분리 — 겹쳐도 둘 다 읽힌다)
>   `.upcard` 은청 금속 · `.fusedcard` 보라 · `.willfinish`는 프레임을 양보하고 **외곽 발광**만. 좌상단에 `+` / `쌍성` 칩 — 부채꼴에서 카드가 겹쳐도 **왼쪽 위 모서리는 항상 보인다**
> - **조력자 = 액자 밖으로**. `.portimg`(원형 마스크·금테·`cover`)는 로그 칩·증강 선택 화면 전용으로 남기고, 게임판에는 **`.allysprite`**: `background-size:contain` · `background-position:50% 100%`(바닥 정렬) · 테두리/라운드 제거 · **`box-shadow` 대신 `filter:drop-shadow`**(사각형이 아니라 실루엣을 따라간다). 발광도 `drop-shadow` 애니메이션
>   배치는 **오른쪽 가장자리 세로 레일**(폭 156px, 인물 높이 `clamp(104, (100vh−210)/3, 188)`), 이름 14px, 능력 설명은 호버 시. 말풍선은 **그 사람 바로 위**(맨 위 인물은 `.down`으로 아래로 뒤집는다)
> - **맵은 틀을 벗는다**: 노드 원형 테두리·배경 폐지, 아이콘만 남기고 **색과 크기로 위계**를 준다 — 전투 30px 은청 · 상점 32 금 · 휴식 32 초록 · 이벤트 32 하늘 · 대장간 34 주황 · **중간보스 50 보라 + 맥동 아우라** · **보스 70 적색 + 맥동 아우라**. `_ic()`가 전부 금색 단색이라 구분이 안 되던 것을 `.mapnode .svgic svg{stroke:var(--nc)}`로 CSS가 이기게 했다. 행 간격 62 → 82px, 맵 폭 400 → 520
> - **드래그 중 규칙**: `body.dragging`에서 부채꼴 회전·호버 확대·용어 패널을 전부 끄고, 드래그 중인 카드만 `transform`으로 손끝에 붙인다(고스트를 따로 만들지 않아 FLIP 애니메이션이 그대로 산다). 드롭 후보는 `.enemy.drophover` 금색 아웃라인
> - **멈블 사운드**: 조력자가 말할 때 성별·연령별 음색으로 짧은 음을 대사 길이에 비례해(3~10회) 찍는다 — 노년 남 104~112Hz square / 중년 남 138~152Hz / 여성 232~246Hz triangle

# 아트 스타일 가이드 v1.0

> v1.0 개정 (2026-08-09, v0.23 빌드) — **별자리 그림을 하나의 데이터로 통일 + 별자리별 완성 소리**
> - 사용자 지적("전투 중 성좌판과 성좌 발동 연출의 별자리가 다르다") 해소. 이제 **카드 앞면 · 전투 성좌판 · 완성 연출**이 전부 `const_lines.json` 하나에서 나온다
>   `gen_const_lines.py`(투영) → `const_lines.json` → `gen_fx_lines.py`가 **CONST_LINES(100×100, 카드용)** 과 **CONST_FX(320×300, 성좌판·연출용)** 를 동시에 주입. **별 인덱스가 완전히 동일**해 카드의 `si`가 세 화면에서 같은 별을 가리킨다
> - **투영을 레퍼런스 방식으로 교체**: 구 방식(적경×cos·적위 직교 배치)은 큰 별자리가 좌우로 늘어났다 → **별자리 중심 입체 투영**(stereographic). 별의 순서·개수는 그대로라 카드는 영향 없음
> - **성좌판도 연출과 같은 배치**: 320×300 뷰박스 + 삽화 상자(별 경계 상자 × adj.s × 0.75)
> - **별자리마다 완성 소리** (`CON_SFX` + `VOICE` 신디사이저, 외부 음원 없음): 사자·큰곰·작은곰 **포효** / 오리온 **기합** / 황소 울음 / 큰개 짖음 / 독수리·까마귀 울음 / 전갈 독침 / 게 집게 / 궁수 활시위 / 페르세우스 베기 / 마차부 말발굽 / 케페우스 북 / 용 화염 / 고래 노래 / 백조 날갯짓 / 페가수스 울음 / 안드로메다 사슬 / 목동 휘파람 / 남쪽물고기 물방울 / 거문고·처녀 하프 / 쌍둥이 화음 / 카시오페이아·왕관 차임. **삽화가 떠오르는 순간**에 맞춰 울린다
> - **조력자 박스**: 어느 화면에서든 **오른쪽 아래 고정 · 항상 펼침**(접기 폐지, 184px). 손패 부채꼴은 박스를 피해 폭 상한 + 중심을 왼쪽으로 옮긴다

# 아트 스타일 가이드 v0.9

> v0.9 개정 (2026-08-09, v0.22 빌드) — **성좌 완성 연출을 사용자 레퍼런스(`Constellation Effect.dc.html`)대로 재제작**
> - **연출 전용 기하 `CONST_FX` 신설** (`gen_fx_lines.py`): 별자리 중심 기준 **입체 투영**(stereographic) → 320×300 상자에 여백 34로 자동 맞춤. 별 반지름 = `max(1.1, 4.8 − 0.62 × 등급)`
>   ※ 카드의 `si` 인덱스는 기존 `CONST_LINES`(100×100)를 그대로 쓴다 — **연출용과 카드용 기하를 분리**해 195장의 별 지정을 건드리지 않았다
> - **3단 모션** (레퍼런스 타이밍 그대로): ① 별이 `scale .3→1`로 하나씩 켜진다(0.6s, 0.05s 간격) → ② 선이 `stroke-dashoffset`으로 순서대로 그어진다(0.8s, `cubic-bezier(.4,0,.2,1)`, 0.09s 간격) → ③ **삽화가 떠오른다**(1.4s ease, 선이 다 그어진 뒤 +0.2s, 최종 불투명도 0.62). 연속 완성 시 전 구간 0.55배로 축약
> - **별 = 3중 원**: 헤일로(r×4.4, 10%) + 중간(r×2.1, 28%) + 코어(r, 100%). 색 `#f4f1e4`(특별 성좌는 `#eaf0ff`)
> - **삽화 배치**: 각인 그림을 화면 가득 채우지 않고 **별 경계 상자 × adj.s × 0.75** 크기의 상자에 넣어 별자리와 겹쳐 놓는다(오리온 1.45 · 카시오페이아 1.7 · 큰곰 1.3 …). 각인이 없는 9개는 `CON_FIG` SVG가 같은 상자에 들어간다
> - 선 색은 게임 규칙대로 **금색**(`rgba(232,197,106,.92)`) 유지 — 레퍼런스의 은청색은 특별 성좌 전용
> - 검수 시트: `성좌연출_v0.22.png` (최종 프레임 12종)
> - **조력자 초상 교체**: 새 `조력자.png`는 배경이 **갈색 그라디언트**(구 흰 배경·한글 라벨 폐지) → `embed_assets.py`가 행/열 분리 대신 **전경 연결 성분 7개를 직접 검출**해 각자 크롭한다
> - **조력자 박스에 `조력자` 라벨**(Cinzel 11px, 자간 3px, 금색 밑줄)

# 아트 스타일 가이드 v0.8

> v0.8 개정 (2026-08-09, v0.21 빌드) — **적 = 도트(픽셀) 스프라이트로 전면 교체** (사용자 지시 "몹들은 그냥 픽셀로 가자")
> - 기획서 v0.2·아트가이드 톤앤매너의 원칙("카드 = 평면 일러스트, **게임플레이 화면 = 도트**")을 드디어 이행. 구 벡터 일러스트(`ENEMY_ART`)는 폴백으로만 남긴다
> - **생성 방식 = 절차적**(`gen_pixel_art.py`): 손으로 찍지 않고 프리미티브(구 셰이딩·크레이터·타원 고리·성운 구름·나선팔·4각 별)를 조합해 격자를 채운다. 그림을 고치려면 레시피만 바꾸고 다시 돌린다
>   `python gen_pixel_art.py <빌드html>` → 빌드의 `const PIXEL_ART={};/*PIXEL_ART_DATA*/` 자리에 주입 (34종 ≈ 27KB)
> - **격자**: 쫄몹·행성 24×24 / 딥스카이·보스 28×28. 팔레트는 스프라이트마다 문자 인덱스(`{p:{문자:색}, r:[행 문자열]}`), `.` = 투명
> - **렌더**: `pxArt(id)`가 같은 색이 이어지는 구간을 런렝스로 묶어 `<rect>` 한 줄로 만든다 → `shape-rendering="crispEdges"` SVG. 벡터라 확대해도 픽셀이 뭉개지지 않는다(스프라이트당 80~200 rect)
> - **광원 통일**: 전 스프라이트 좌상단 광원. 구체는 밝음/기본/어둠 3톤 + 필요 시 림라이트. 색은 실제 천체 사진의 대표색을 기준으로 잡되 밤하늘(#0b1026) 위에서 뭉개지지 않게 채도를 올린다
> - 산출물 미리보기: `도트천체_v0.21.png` (34종 시트)
> - **조력자 말풍선 확장**: 전투 밖(맵·상점·휴식·대장간)에도 `.allyquote` 칩으로 대화가 붙는다 — 초상 28px + 이름 + 대사, 2인 이상이면 주고받는 두 줄

# 아트 스타일 가이드 v0.7

> v0.7 개정 (2026-08-09, v0.20 빌드) — **실사 에셋 2종 파이프라인 + 화면 밀도**
> - **조력자 초상 7인**(`조력자.png` → `embed_assets.py`): 흰 배경 flood fill → **셀에서 가장 큰 연결 성분만 남겨 한글 라벨 자동 제외** → 1px 침식·페더 → 긴 변 340px WebP(알파). 원형 마스크(`.portimg`)로 조력자 박스(86px)·증강 선택(116px)·대사 칩(22px)에 사용. 실사가 없으면 기존 SVG 초상으로 폴백
> - **계절 배경 4장**(`배경.png` 2×2 → 같은 스크립트): 흰 구분선·금테 안쪽 크롭 → 가로 960 WebP. **전투 화면에서만** 캔버스에 cover로 깔고 `rgba(6,8,20,.52~.80)` 세로 그라디언트로 덮는다 — 측정 평균 밝기 **53**(카드·UI 가독성 기준). 계절이 바뀌면 기존 `bgFade`로 크로스페이드. **1막은 이미지 없음** = 멈춘 하늘
> - **몹 크기**: 일반 244px(아트 176) · 보스/엘리트 **296px**(아트 226) · 쫄몹 156px(아트 112). 적이 3마리 이상이면 `#enemyRow.many`로 자동 축소
> - **카드 용어 패널**(`.hpanel`): 손패 슬롯의 **형제**로 붙여 카드 확대에 딸려가지 않게 한다. 오른쪽 236px, 화면 끝 카드는 `.flip`으로 왼쪽 반전
> - **조력자 말풍선**(`.allybark`): 박스 왼쪽에 260px, 3.2초 후 페이드. 2인이면 1.5초 간격으로 주고받는다
> - **비교 컴포넌트**(`.cmpRow`): 대장간 합성(재료+재료→결과)과 강화 미리보기(지금→강화 후)가 같은 틀을 쓴다. 바뀐 수치는 `.difftag` 초록 태그

# 아트 스타일 가이드 v0.6

> v0.6 개정 (2026-08-09, v0.19 빌드) — **카드 확대 + 텍스트를 기호로**
> - 카드 156×272 → **178×308** (메달 30→34 · 비용 육각 22→25 · 아트창 92→106 · 이름 11.5→13px). 손패 슬롯 132×222 → 152×252
> - 손패 전환 곡선 `cubic-bezier(.16,.84,.24,1)` .34s — 미끄러지듯. 호버 확대 1.42 → **1.36**(과장 축소), 상승 122 → 140px
> - **카드 텍스트 = 기호 + 짧은 말**: 키워드 SVG 14종(`ART.kw` — 피해·관통·성막·뽑기·물질·회복·중독·화상·약화·취약·가시·봉인·버리기·전체)을 앞에 붙이고, 키워드 정의는 **툴팁**으로 내린다. 문장형(v0.15)은 카드가 글자로 꽉 차 폐기
> - **특수 메커니즘 이름 인장**(`.fxseal`, 카드 이름 옆 금색 배지): 전차·처형·수확·대관·변광·램프·파워·봉인·교대·선택·복제·길잡이·버리기·예열·가속·전조·쌍성·겸용
> - 성좌 조건 줄은 `✦ 한 턴 2성 → 보너스`로 압축
> - **계절 하늘**(2막): 계절마다 배경 색조 + 떠다니는 입자가 바뀐다 — 봄 꽃잎(분홍) / 여름 반딧불(청백, 명멸) / 가을 낙엽(주홍, 회전) / 겨울 눈(은백). 전환 순간 화면 섬광 + 계절 표시 번쩍임. **1막은 계절이 없어 입자도 없다**
> - **별자리 선택 패널**(`.conpick`): 보상 화면에서 각인 그림 + 이름 + 아이덴티티 + 완성 보너스 + 보유 수를 한 장에. 보스 유물 패널은 같은 틀에 보라 테두리

# 아트 스타일 가이드 v0.5

> v0.5 개정 (2026-08-09): **성좌 각인 그림 = 누끼(배경 제거) 개별 이미지 방식 확정**
> - 원본 `성좌각인.png`(3행×[6·6·5], 세피아 각인풍)의 **배경을 프로그램으로 제거**해 별자리별 개별 WebP(알파)로 임베드한다. `embed_conart.py` 한 번 실행이 전 과정: 테두리 flood fill(이웃 대비 허용오차 9) → 도형 안 갇힌 배경 주머니 제거 → 1px 침식 + 페더 → 3행/6·6·5칸 자동 분리 → 여백 트림 → 긴 변 460px 리사이즈 → WebP q80 → base64 주입
> - **밤하늘(#0b1026) 위에 얹었을 때 사각 배경도, 배경색 후광도 없어야 한다**가 합격 기준. 그림을 교체할 때도 같은 스크립트만 다시 돌린다
> - 표시 방식: 시트 크롭(`background-size:600% 300%`) 폐지 → `background-size:contain`으로 **원본 비율 유지**. 사용처 = 성좌 완성 연출 형상 · 중앙 성좌판 배경 · 도감 그룹 헤더 썸네일
> - **각인 그림은 17개 별자리분만 존재** — v0.18 신규 9개(궁수·왕관·까마귀·게·마차부·페르세우스·케페우스·용·고래)는 `CON_FIG` 수제 SVG 라인아트가 폴백. 신화 모티프: 활 당긴 켄타우로스 · 아리아드네의 관 · 아폴론의 까마귀 · 집게 · 염소를 안은 마부 · 하르페와 메두사 머리 · 왕관 쓴 왕 · 감긴 라돈 · 바다괴수 케토
>
> 디자인팀 · 사용자 아트 지시(카드 템플릿 이미지·전갈자리 일러스트 예시) 반영
> v0.4 개정: ① 별자리 선을 **실제 별 좌표(J2000 적경·적위) 투영**으로 재생성 — 나무위키/성도 표준 모양과 일치 (`const_lines.json`, 17개 별자리) ② **카드 템플릿 전면 재설계** (사용자 제공 골드 프레임 템플릿 기준) ③ 성좌 완성 연출에 **별자리 그림 단계** 추가
> v0.3 개정: 표준 별자리 선 방식 채택, 완성 연출 신설, 계절 태그 팔레트

## 톤 앤 매너

밤하늘 관측의 차분한 경외감. 짙은 남색(#0b1026)~검정 배경 위에 금색(#e8c56a) 성좌선과 흰 별. 화려함보다 가독성과 정갈함 우선. 카드 = 평면 일러스트, 게임플레이 화면 = 도트(픽셀) — 기획서 v0.2 11장 확정.

## 카드 레이아웃 (v0.4 — 사용자 템플릿 확정)

사용자 제공 골드 프레임 템플릿을 기준으로 한다. 배치는 위에서부터:

1. **좌상단 원형 메달**: **계절 아이콘** — 봄 🌸 벚꽃 / 여름 ☀️ 태양 / 가을 🍁 단풍 / 겨울 ❄️ 눈꽃 / 주극 ⭐ 북극성. 메달 배경은 계절 성운색 소용돌이.
2. **메달 아래 육각형**: **비용** (성간 물질 수치).
3. **상단 가로 직사각형(검정)**: **카드 제목** (별 이름 · 소속 별자리 부기).
4. **중앙 대형 아트 창**: **소속 별자리의 표준 별선** (해당 별 강조 + 은하수 그라데이션 배경).
5. **아트 아래 중앙 보석**: **희귀도 보석** — 기본 = 은회색 보석 / 일반 = 파랑 사파이어 / 희귀 = 보라 자수정 / 전설 = 금색 보석. 보석 색이 곧 등급 표기.
6. **파란 직사각형**: **기본 효과** (냈을 때 효과).
7. **하단 자주/붉은 직사각형**: **성좌 효과** (성좌 완성 시 효과).

전체 프레임은 금색 장식 테두리(코너 장식 포함). 본문 글자 14px 이상. 플레이버는 카드 도감에서 제공.

## 색 체계

- 희귀도 프레임: 기본 #9aa4b8(은회색) / 일반 #5db3d9(청색) / 희귀 #b07fe8(보라) / 전설 #e8c56a(금색). **전설 = 황도 12궁 전용** — 금테가 곧 황도의 상징.
- 계절 성운(아트 창 배경 그라데이션, 구 원소 색 대체): 봄 #8fbf6f / 여름 #3f9fbf / 가을 #bf7f4a / 겨울 #7f9fdf / 주극 #c9d4e8(은백 — 사계절 불변).
- 성좌선은 항상 금색 반투명(rgba(232,197,106,.75)), 별은 흰색 + 글로우. 특별 성좌(대삼각형·대사각형) 선은 은백(#c9d4e8)으로 구분.

## 별자리 아트 규칙 — 실좌표 표준 성도 방식 (v0.4 확정)

별선은 눈대중이 아니라 **실제 별의 J2000 적경·적위 좌표를 투영해 생성**한다 — 나무위키·교과서 성도의 모양과 자동으로 일치한다.

- 데이터: `const_lines.json` — 17개 별자리의 별 좌표(100×100 뷰박스 정규화)·표준 연결선·별 이름. 생성 스크립트는 `gen_const_lines.py` (새 별자리 추가 시 좌표만 추가하고 재실행).
- **큰곰자리는 전신 18성** (2026-08-06 사용자 지시 — 북두칠성 7성만으로는 "큰곰"이 아니라는 지적 반영): 국자 7성 + 머리(무시다·23번성·웁실론·피)·앞다리(세타·탈리타·카파)·뒷다리(카이·프사이·람다·뮤). 연결선은 Stellarium modern 표준 성도 기준.
- 연결은 성도 통용 연결만 사용 (형상 연출용 임의 선 금지). 밝은 별(시리우스, 베텔게우스, 베가, 안타레스)은 반지름·글로우 강조.
- 프로토타입의 별선 데이터 객체 `CONST_LINES`를 이 JSON으로 교체 (제작팀).

## 성좌 완성 연출 (v0.4 — 별자리 그림 단계 추가)

성좌 완성 순간의 4단계 연출 (사용자 예시: 전갈자리 일러스트처럼 별선 위에 형상 그림이 함께 뜨는 방식):

1. **점멸**: 이번 턴에 낸 해당 별자리 카드의 별들이 밝게 점멸
2. **선 잇기 모션**: 표준 연결선이 별에서 별로 **순서대로 그어지는** 모션 (0.5~1초, stroke-dashoffset)
3. **별자리 그림 떠오름**: 완성된 별선 위로 **그 별자리의 형상 그림**이 함께 떠오른다. **v0.15: 그리스 신화풍 재작**(사용자 지시 "웅장하게") — 다중 패스 실루엣(외곽 2.2/디테일 1.2 이중 굵기, 은백 선 + 금색 포인트): 곤봉·사자 가죽 방패의 오리온, 앞발 든 큰곰(칼리스토), 옥좌의 카시오페이아, 창을 든 쌍둥이, 날개 편 정의의 여신 처녀, 사슬에 묶인 안드로메다, 벼락 문 독수리 등 17종. 공통 장식 = **점선 후광 링 + 월계 가지**(`_laurel`). 도감 그룹 헤더·좌측 효과 피드의 완성 칩에도 동일 형상. **2차 시안 — 사용자 평가 대기**
4. **보너스 표기**: 별자리 이름 + 완성 보너스 배너

특별 성좌는 은백 선(#c9d4e8)으로 구별. 연속 완성은 큐 처리 + 축약 가능. 도트 화면에서는 픽셀 선으로 재현.

## 산출물

- 카드 비주얼 목업: `카드비주얼_초안_v0.2.html` — **표준 별선(`CONST_LINES`)·계절 오버레이 기준으로 갱신 필요** (레이 방식 별선은 폐기 대상)
- 전투 프로토타입: `제작/builds/전투프로토타입_v0.1.html` — 동일 데이터·연출 반영은 제작팀 작업
