# Starry Night — 별자리 카드 로그라이크 덱빌더

> 게임명 = **Starry Night** (확정 — 게임 내 타이틀·문서 전부 이 이름 사용. 구 가칭 "성좌 전쟁" 폐기)

별지기가 개별 "별" 카드로 덱을 짜고, 같은 별자리의 별을 한 턴에 필요 수만큼 내면 성좌가 완성되는 턴제 로그라이크 덱빌딩 게임. 적은 "별자리에 속하지 못해 소외된 천체들"(행성·위성·성단·블랙홀).

## 파일 안내

- `전투프로토타입_v0.31.html` — **현재 빌드** (단일 HTML 약 1.25MB, 브라우저로 열면 플레이). 시작 화면: 튜토리얼 / **1막 → 2막 → 3막 여정** / 보스전 단판 / 도감 111종. **드래그 전용 발동**·바이어 등급·담기/버리기 보상·도트 천체 34종·대장간 합성·보스 기믹 9종·적 계절 보정·조력자 3인 수다·이벤트 18종·유물 22종. 구버전 v0.5~v0.30 보존 ※ v0.29·v0.30은 원격에서 `starry-night-v0.xx.html`로 먼저 만들어졌다 — **원본 PC에서 한글 파일명 사본을 복사로 만들 것**
- `Constellation Effect.dc.html` + `gen_fx_lines.py` + `성좌연출_v0.22.png` — **성좌 완성 연출 레퍼런스**(사용자 제공)와 기하 생성기. `const_lines.json` 하나에서 **CONST_LINES(카드용)·CONST_FX(성좌판·연출용)** 를 동시에 주입해 **세 화면의 별자리를 통일**한다(별 인덱스 공유). **별자리 그림을 손보려면 `python gen_const_lines.py` → `python gen_fx_lines.py <빌드html>` 순서로 재실행**
- `gen_pixel_art.py` + `도트천체_v0.21.png` — **적 도트 스프라이트 생성기**(절차적). 24×24(쫄몹)·28×28(보스) 격자를 프리미티브로 그려 런렝스 SVG로 주입. **적 그림을 바꾸려면 이 파일의 레시피만 고치고 `python gen_pixel_art.py <빌드html>` 재실행**
- `조력자.png`(입다뭄, 기본) · `조력자2.png`(입엶) · `배경.png` + `embed_assets.py` — 조력자 초상 7인 **2프레임**(v0.31 입 벙긋용 — 두 시트를 짝지어 같은 상자로 잘라 `PORTRAIT_IMG`/`PORTRAIT_OPEN` 주입, 입엶 시트의 조각난 성분은 기본 시트 인물 중심에 귀속)과 계절 배경 4장. **그림 교체 시 png 갈아끼우고 `python embed_assets.py <빌드html>` 재실행**
- `성좌각인.png` + `embed_conart.py` — 성좌 각인 원본(3행×[6·6·5]칸)과 **누끼+분할+임베드 스크립트**. 배경 제거(테두리 flood fill + 갇힌 주머니 제거 + 1px 침식) → 17장 트림 → WebP(알파) → base64 주입. **그림 교체 시 png 갈아끼우고 `python embed_conart.py <빌드html>` 재실행**
- `게임기획서_v0.2.md` — 기획서 (스토리, 전투, 계절, 성좌 완성, 로드맵) ※ 로컬 미보유 — 확보 시 복원
- `카드목록_v1.2.md` — **별 111종**·별자리 26개·**바이어 등급표(rank 1~5)**·별자리별 전 카드. 키워드 사전 14종은 불변 (구 v0.6~v1.1 보존)
- `밸런스로그.md` — 밸런스 결정 이력 (근거 포함). 3차 = 시뮬레이션 결과, 4차 = 사용자 확정
- `sim_battle_v2.py` + `sim_tune.py` + `sim_bosses.py` + `dump_sim_data.js` + `sim_data_v025.json` — **밸런스 시뮬레이터 v2**. 빌드 HTML에서 데이터를 그대로 떠(`node dump_sim_data.js <빌드> sim_data_v025.json`) 현행 규칙을 재현한다. `python sim_battle_v2.py --validate` · 전 구간 `python sim_tune.py --final` · 수급 A/B `--supply` · 시작 덱 `--start` · **조우 시점 덱 기준 보스 승률 `python sim_bosses.py`**(전 구간 클리어율은 표본이 한 자릿수라 노이즈 — 판정은 보스 승률로). 결과 요약은 `sim_final_report.txt`. **구 v1은 폐기**
   ※ 시뮬 절대 승률은 **하한선** — 그리디 AI는 사람보다 덱을 못 짠다 (밸런스로그 13차)
- `아트스타일가이드.md` — 카드 템플릿, 표준 별선, 성좌 완성 4단계 연출 스펙
- `개발현황.md` — 구현 상태, 기술 결정, 다음 할 일
- `const_lines.json` + `gen_const_lines.py` — 별자리 선 데이터 (실제 J2000 좌표 **별자리 중심 입체 투영** — v0.23에서 교체). 큰곰은 전신 18성(Stellarium modern 표준). py는 현재 uma + v0.18 신규 9개 좌표 보유 — 새 별자리는 py에 좌표 추가 후 재실행
- `PRODUCT.md` — 제품 진실 (Impeccable `init` 산출물: 사용자·목적·포지셔닝·제약·브랜드 약속·미확정 항목)
- `.impeccable/critique/` — 디자인 크리틱 스냅샷 (v0.25 = 19/40). `polish`가 이걸 백로그로 읽는다
- 팀 스킬(디자인팀·제작팀·밸런스팀)은 **`constellation-game-studio` 플러그인**으로 설치돼 있다 — 저장소가 아니라 Claude Code 쪽에 붙어 있으므로, **새 기계에서는 플러그인을 따로 설치**해야 "제작팀 소집"이 먹는다
- **GitHub**: <https://github.com/leedohyang15-design/starry-night> · 플레이 <https://leedohyang15-design.github.io/starry-night/>

## 두 대 이상에서 작업할 때 (2026-08-10)

- **시작 전 `git pull`, 끝나면 바로 `git push`.** 게임이 1.25MB 단일 HTML 한 파일이라 양쪽에서 동시에 고치면 git이 병합하지 못하고, 사실상 수동 복구가 불가능하다. **두 곳에서 동시에 만지지 않는다.**
- 저장소에 **없는 것**: 구버전 빌드 27개 · 구 카드목록 · 구 시뮬 스냅샷 · `.claude/settings.local.json`. 되돌아갈 구버전이 필요하면 원본 PC에만 있다
- 저장소의 게임 파일 이름은 **`starry-night-v0.31.html`**(ASCII)이다 — GitHub Pages URL 인코딩을 피하려고 영문으로 두었다. 로컬 `전투프로토타입_v0.31.html`과 내용은 같다. **새 버전을 낼 때 둘 다 갱신할 것** (구버전 v0.28~v0.30도 저장소에 보존)
- git 저장소가 OneDrive 폴더 안에 있다. OneDrive가 `.git`의 작은 파일들을 붙들어 저장소를 깨뜨릴 수 있으므로, **다른 기계에서는 OneDrive 바깥에 clone**할 것

## 기술 표준 (반드시 유지)

- 단일 HTML 파일 (HTML+CSS+JS 인라인), 외부 스크립트는 cdnjs만, **localStorage 사용 금지**
- **데이터 주도**: 수치·효과는 상단 `STARS`/`CONSTELLATIONS`/`ENEMIES`/`CONST_LINES` 객체에만. 로직에 하드코딩 금지
- 검증: 수정 후 브라우저로 JS 에러 0 + 핵심 시나리오 스모크 테스트. **버튼은 함수 직접 호출이 아니라 `elementFromPoint`로 실제 클릭 지점을 찍어 확인할 것** (v0.25에서 조력자 스프라이트가 턴 종료 버튼을 가린 걸 놓쳤다). 세로 720px 이하 창도 함께 볼 것. 버전은 semver로 올려 새 파일로 (기존 버전 파일 덮어쓰지 않기)
- 모바일 터치 대응 유지
- **화면 맞춤은 `fitBoard()`가 잰다**(v0.26): 창이 좁으면 카드를 자르지 않고 `--card-scale`로 줄이고, 그래도 넘치면 성좌판을 접되 **적이 잘리지 않는 선**까지만. CSS `calc`로는 무단위 비율을 못 만들고 `document.scrollHeight`는 하단 여백까지 세므로, 기준은 **`#deskPanel` 하단**이다
- **조력자 레일은 `pointer-events:none`**(v0.26): 호버 타깃은 이름표(`.aname`)뿐. 실루엣이 뒤 버튼을 삼켰다
- **로그는 `log(msg,{kind,icon:{type,id},life,quiet})` 한 계약만 쓴다**(v0.30): 아이콘에 raw HTML 금지(`feedIcon`이 렌더), 이력은 `S.history` 배열(전투 기록 패널 `openHistory`), 화면 전환 시 `feedClear()`. kind = card/hurt/hit(플레이어 피격)/con/ally/item/boss/turn
- **맵 캡션은 호버·포커스 전용**(v0.30 — 구 상시 라벨 폐지), 선은 3상태(지나온 길·개척선 `.e-front`·나머지), 열린 노드는 tabindex 0
- **전역 z-index는 `:root`의 `--z-*` 토큰만 쓴다**(v0.29): 맨 숫자를 새로 만들지 말 것. 그리고 **`position:relative`에 z-index를 얹으면 스택 문맥이 생겨 자식의 z-index가 그 안에 갇힌다** — `#playerBar{z-index:2}`가 `#endTurn{z-index:26}`을 가둬 좁은 창(≤414px)에서 손패 호버 카드가 턴 종료 버튼을 덮었다 (v0.26 조력자 레일 사건과 같은 계열, v0.29에서 수정)

## 핵심 규칙 요약

- 마나 = 성간 물질 3/턴, 드로우 5, HP 70
- 계절: 턴마다 봄→여름→가을→겨울 순환. 제철 +25%(올림) / 역철 −25%(내림) / 주극성 무보정. 수치 효과만, 다단히트는 총합 기준. **1막 = 계절 없음(보정 0, `R.noSeason`) / 2막·3막 = 순환**(관문마다 시작 계절이 한 칸 넘어감 `R.seasonStep`) — 계절마다 배경 색조·입자(꽃잎/반딧불/낙엽/눈)가 바뀐다
- 성좌 완성: 별자리마다 필요 수 다름 (1~3성, 분포 **1×1·2×16·3×9** — v0.20에서 3성 9개 부활, 구 v0.19 분포 1·23·2 폐기), **한 턴에** 필요 수를 채워야 하고 **발동은 턴당 성좌마다 1회** (2026-08-08 최종 — 다중 완성 폐지: 북극성 −1과 결합한 연쇄 발동 밸런스 붕괴 차단), 같은 별 중복 인정, 미자르-알코르 = 2성 계산, 북극성 = 필요 수 −1 (하한 1). **"낸 별 수" 램프 효과(오리온 등)는 별도의 전투 누적 카운터**(`battleCounts`) 사용
- **카드 등급 = 별자리 안에서의 그리스 문자 순위**(v0.25 사용자 확정 — "a가 가장 좋은 카드"). `STARS[id].rank` 1~5, α가 으뜸. `GRADE_NAME`(으뜸별~다섯째별)·`GRADE_CLR`(금·보라·하늘·초록·은회)·`GRADE_W`(0.16/0.34/0.60/0.85/1.0)·상점가(110/82/62/46/34)·위력 예산(150/134/122/110/100%). 카드 제목의 그리스 문자와 우하단 등급 구슬이 **같은 색**을 쓴다. 구 기본/일반/희귀/전설은 폐기
- **별자리당 카드는 2~5장**(v0.25 사용자 지시): **3성 5장 · 2성 4장 · 1성 2장 = 111종**. 선별은 "고유명 있는 별을 그리스 문자 순으로", 예외 2곳(큰곰 5위 = 미자르-알코르 ζ · 전갈 5위 = 샤울라 λ). **문자는 건너뛰어도 순서는 절대 뒤집히지 않는다**
   ※ 축소할 때 수치를 다시 매기지 않고 **별자리 안에서 효과 페이로드를 rank 순으로 재배치**했다(별 고유 정보 `by·name·si·countAs·alsoCon`은 별에, 효과 `type·cost·target·fx·comp·extraTxt`는 이동). 별자리 총 전력이 보존돼 보스 승률이 ±3%p 안에 머물렀다 — 밸런스로그 17차
- **카드 수급 = 별자리 단위**: 전투 보상은 **별자리 3택1 → 그 별자리에서 등급 가중 무작위 3장 → 그중 원하는 만큼만 담기**(v0.25 사용자 확정 — 덱 비대화를 플레이어가 직접 조절). 가중치는 **풀에 직접** 건다. **3택 중 둘이 이미 모으던 별자리**(v0.25, 구 하나)이고, **지금 계절 별자리가 3배 자주** 제안된다(`SEASON_BIAS`). 상점·이벤트 추첨도 같은 `wpick`
- **덱 줄이는 수단**: 휴식 '지우기' · 상점 제거 2회(50·80 가루) · 상점 강화 서비스(70) · 이벤트 3종 · 대장간 합성(−1장). 덱 하한 5장. **휴식 선택지 = 휴식/강화/지우기/성도 보기**(v0.30 — 합성 제외)
- 성좌 필요 수: **1성×1 · 2성×16 · 3성×9**(오리온·큰곰 + 황도 7). 3성은 **대장간 합성**(1장이 2성) 덕에 현실적이 됐다 — 합성 1쌍이면 완성률이 4배로 (밸런스로그 14차)
- 1막 시작 덱 = **주극성 9장**(v0.25 — 카시오페이아·큰곰·작은곰 각 3장, α는 제외). 9장이면 매 턴 덱의 절반 이상을 보므로 처음부터 성좌를 이을 수 있다
- **대장간**(2026-08-09 사용자 지시, 구 '합성 폐기' 결정 번복): 같은 별자리 2장 → 1장 · **비용 = 높은 쪽 · 효과 둘 다 · 성좌 2성 계산 · 재합성 불가**. 맵 대장간 노드 **1개**(v0.21에서 3 → 1 축소 — 사용자 지시). **휴식의 '합성' 선택지는 v0.30에서 폐지**(사용자 지시 "합성 카드가 너무 많다") — 합성은 대장간 노드와 2막 이벤트 '위성의 조언'에서만. 밸런스 영향은 밸런스로그 19차. ※ 덱 항목은 `{id,up,id2,up2}` — **전투 진입 시 id2/up2를 반드시 보존**할 것(v0.20에서 이걸 흘려 합성이 무효였다)
- **적도 계절을 탄다**: 딥스카이 6종에 보이는 계절(M42·플레이아데스·히아데스·M1=겨울 / M13·블랙홀=여름) — 제철 공격력 +25% / 역철 −25%. 2막은 관문마다 시작 계절이 봄→여름→가을→겨울로 넘어간다
- **보스 기믹**: 플레이아데스 분열 · 히아데스 흐린 하늘(성좌 봉인) · M42 성운의 장막(손패 봉인)+2페이즈 · M1 펄서 파동(성막 흡수) · M13 밀집(피해 −20%) · 블랙홀 사건의 지평선(손패 흡수)+계절 역행+2페이즈
- **별자리별 성좌 완성 소리**(v0.23): `CON_SFX` 표 + `VOICE` 웹오디오 신디사이저 26종 — 사자 포효 · 오리온 기합 · 용 화염 · 고래 노래 등. 삽화가 떠오르는 순간 울린다
- **조력자는 액자 없이 오른쪽 가장자리에 세로로 선다**(v0.24 — 누끼 그대로, `.allysprite`). 모든 화면에 표시. **말풍선은 흰 바탕 검은 글자 15.5px**, 그 사람 바로 위(위/아래는 붙인 뒤 실제 rect로 판정). **대사는 말풍선에만 나오고 로그(피드)에는 안 흘린다 — 피드에 기록되는 조력자 항목은 능력 발동뿐**(v0.30, `PORTRAIT_IMG` 초상으로 통일. 구 SVG 초상 `ART.portrait`는 삭제됨). **전투 밖(맵·상점·휴식·대장간) 대사도 같은 말풍선**을 쓴다 — 구 화면 상단 인라인 대사(`allyQuote`/`#mapTalk`)는 v0.25에서 폐기. 대사마다 성별·연령별 **멈블 사운드**(`VOICE_TONE`). **말할 때 입 벙긋**(v0.31 — 말풍선 동안 `PORTRAIT_OPEN` 프레임 135ms 교차, `mouthTalk`) + **대기 들썩임**(`idlebob` steps(1), 인원별 박자 어긋냄)
- 조력자 **7인**(갈릴레오·브라헤·허셜·케플러·코페르니쿠스·허블·리비트) — **막마다 1인씩 합류해 최대 3인**(`R.ally`/`ally2`/`ally3`). 상황 15종 × 7인 대사 222줄 + 짝 대화 35쌍, 3인이면 세 번째가 끼어든다
- **보스 유물 6종**(혼천의·북극성의 파편·영원한 성막·초신성의 심장·은하수 렌즈·황도의 인장) — 보스 격파 시 3택1, 상점·중간보스 풀에서 제외
- 상점 시세는 **막마다 오른다**(1막 1.0 / 2막 1.15 / 3막 1.3). 유물 '은하수 지도'는 20% 할인
- 소모품 **13종**(한도 5) · **유물 22종**(일반 16 = 상점·중간보스·이벤트 / 보스 6 = 보스 격파 3택1) · **이벤트 18종**(공통 12 + 막별 2씩, 한 여정 중복 금지). 유물 획득은 반드시 `takeRelic()` 경유(항성 나이테처럼 얻는 즉시 효과가 있다)
- 별 **111종**·별자리 **26개** (2026-08-10 축소): 별자리별 아이덴티티 통일 — 오리온 램프·큰곰 방어·카시오페이아 공격·궁수 관통·마차부 전차·용 광역화상 등 (카드목록_v0.9.md). 효과 중복 금지 원칙은 "같은 별자리 내"로 완화. 계절 분포 봄5·여름6·가을5·겨울5·주극5 (2막 계절 순환 대응)
- **2막**(2026-08-09): 1막 보스 격파 → 덱·유물·아이템·가루 유지한 채 2막 (HP 15 회복, 가루 +80) · **계절 순환** · 쫄몹 = 외행성·거대 위성·떠돌이 행성·오르트 파편 · 중간보스 = 게 성운 M1(115/10)·헤르쿨레스 구상성단 M13(106/9 소환형) · 보스 = **은하핵의 블랙홀**(145/13, 아머 12/3턴 + **중력** 4턴마다 다음 턴 물질 −1) · 보상 가루 1.5배 · **조력자 2인째 선택**
- 1막: 시작 덱 = **주극성만 9장** (카프·치흐·루크바·메라크·페크다·미자르-알코르·코카브·페르카드·이을딘 — 3별자리 × 3장, α 제외, v0.25에서 12장 → 9장. 계절 테마 선택 폐지, "주극으로 시작해 나중에 카드를 추가하는" 성장 곡선) · **1막은 계절 자체가 없음**(보정 0 — v0.19) · 맵 = **3열×13행 + 휴식 + 보스 (관문 15개)** — 특수 노드 상점3·휴식5·대장간1·이벤트4, 조우 3티어(0~2행 쉬움/3~8행 보통/9행~ 강함) · 쫄몹 = **행성·위성·혜성·파편** · 보스급 = **딥스카이** — 중간보스 구간별 3회(플레이아데스 성단 80/8 소환형 · 히아데스 성단 95/9), 최종 M42 성운 **92/9** 하향치 + 격노 상한 +6 (단판 보스전은 원래 140/12) · **적 아머**: 보스·중간보스가 주기적으로 성막(M42 10/4턴 · 플레이아데스 8/4턴 · 히아데스 10/3턴) — **관통이 이를 무시** (아다라·독수리 보너스 활성화), 중독·화상 도트도 아머 무시 · **아이템 이원화**: 소모품 13종(포션형, 최대 5개, 전투 사용) + **패시브 유물 12종**(일반 6 = 상점·중간보스 드랍·이벤트 / **보스 6 = 보스 격파 3택1**) · **조력자 = 천문학자 위인 7인** 런 시작 시 1인 선택(홀로 출발 가능) · 강화/제거 픽커·**덱 보기**(맵·상점·휴식·보상)는 카드 일러스트 그리드 · 전투 모션(카드 비행·피격 셰이크·적 돌진·부채꼴 손패 + 호버 확대) · 전투 UI = 와이어프레임 배치(계절 좌상·아이템/유물 우상·적 중앙·효과 발현 창·물질/HP/성막 중단·덱/손패/버림 하단·조력자 우측) · 배경 = 천구 북극 중심으로 회전하는 계절 별자리 밤하늘
- **3막**(2026-08-09 신설): 2막 보스 격파 → 3막. 쫄몹 = 백색왜성·중성자별·마그네타·암흑 성운·초신성 잔해·유령 은하·마젤란 조각(**전부 계절을 탄다**) · 중간보스 = 안드로메다 은하 M31(청색 이동 = 매 턴 공격 누적 + 위성 은하 소환)·퀘이사 3C 273(**제트** — 3턴마다 성막 관통 + 손패 2장 봉인) · 보스 = **암흑 물질** 880/26(**은폐** 공격 수치 비공개 · **무명** 4턴마다 다음 턴 모든 성좌 봉인 · 2페이즈) · **막 고유 규칙 = 여명**: 전투 14턴째부터 매 턴 성막 무시 피해가 3,5,7…로 커진다 · 보상 가루 2배 · 클리어 = 여정 완주
- **적 그림 = 도트(픽셀)** (2026-08-09 사용자 지시): `gen_pixel_art.py`가 34종을 절차적으로 생성 → `PIXEL_ART` 주입, `pxArt(id)`가 런렝스 SVG로 렌더. 구 벡터 `ENEMY_ART`는 폴백
- 환율: 물질 1 = 피해 6 = 성막 5 = 뽑기 1장. 등급 예산: 5위 100 / 4위 110 / 3위 122 / 2위 134 / **1위(α) 150%**
- **카드 이름 = "별자리 + 그리스 문자"** (v0.24 사용자 확정): `STARS[id].by`가 그리스 문자, `name`은 고유명(없으면 필드 없음) → 표시는 `작은곰자리 α (북극성)` · `황소자리 θ²`. 코드에서는 `cardName(d)`(전체) / `cardShort(d)`(좁은 칸)만 쓸 것 — `d.name`을 직접 찍지 말기
- **강화·합성은 프레임으로 구별**: 강화 = 은청 프레임 + `+` 칩(이름에 '+'를 붙이지 않는다) · 합성 = 보라 프레임 + `쌍성` 칩 · 완성 가능 = 외곽 발광(`willfinish`). **칩은 우상단**(좌상단은 계절 메달 자리 — v0.25에서 이동). **합성 카드는 강화할 수 없다**. **합성 시 같은 효과는 `mergeFx`로 병합** — 약화·취약은 **합산(상한 4턴)**, 다단히트는 피해·관통이 같을 때 횟수 합산, 누적·변광·주사위·처형류는 병합 금지(문구만 `×2`로 접는다)
- **카드는 드래그로만 낸다**(v0.25 사용자 확정 — 하스스톤식). **드래그는 '낸다'까지만이고 대상은 그 다음 지정 화면에서 고른다**(v0.27 사용자 지시 — 드래그로 곧장 대상을 찍지 않는다): 판 아무 곳에 놓으면 → 판이 어두워지고 적에 `겨눔 1·2·3` 배지가 붙는다 → 적을 클릭(또는 숫자키)하면 발동. **살아 있는 적이 하나뿐이면 지정 화면을 건너뛰고 바로 발동한다**(v0.28 — 고를 것이 없는데 클릭을 요구하지 않는다). Esc·배경 클릭이면 카드가 손으로 돌아온다. 대상이 없는 카드는 드롭 즉시 발동.
- **키보드 대체 경로**(v0.26~27): 숫자키 1~9로 카드를 내고, **지정 화면에서는 숫자키가 적을 고른다** · Space/E 턴 종료 · Esc 취소. 8px 이상 끌면 드래그로 승격, 대상 카드는 **적 위에**(적 줄 근처면 가장 가까운 적을 잡아 준다), 나머지는 게임판 아무 곳에 드롭. 손패 위에 놓으면 취소. **클릭은 카드 확대 보기**(`zoomCard`)이고 카드를 내지 않는다 — `copy/sac/discard` 모드의 '고르기'만 클릭. 선택형(`choice`) 카드는 **놓은 뒤** 선택지가 뜬다
- 키워드는 카드목록의 사전 14종 밖으로 늘리지 않기

## 팀 공통 원칙 (사용자 지시)

1. **사용자가 시킨 것만 한다.** 새 아이디어는 임의 적용하지 말고 "제안"으로 분리해 물어본다.
2. **작업 시작 전 반드시 프로젝트 md 문서를 전부 읽는다** (CLAUDE.md·카드목록·밸런스로그·개발현황·아트가이드 + 기획서 확보 시 기획서). 문서에 이미 정해진 설정(조력자·명칭·수치 등)을 임의 창작으로 대체하는 것 금지. 문서에 없는 결정이 필요하면 사용자에게 묻는다. (2026-08-07 지시 — 조력자를 기획서의 "천문학자 위인" 설정 대신 임의 창작한 사고의 재발 방지)

## 확정된 결정 (2026-08-06~07)

0. **게임명 = Starry Night** · **조력자 = 천문학자 위인들** (기획서 설정 — 갈릴레오·튀코 브라헤·캐럴라인 허셜로 구현, 구 임의 창작 "꼬마곰 폴라" 폐기)
1. 버리기 기믹 이름 = **"버리기"** (구 가칭 "제물" 전면 교체)
2. 성장 시스템 = **강화 + 대장간 합성** (2026-08-09 사용자 지시로 '합성 폐기' 번복 — 계승은 여전히 폐기). 강화 1회 ≈ 별빛 0.5개 가치
3. 상점 재화 이름 = **"별빛 가루"**
4. 특별 성좌(대삼각형·대사각형) = 추후 **이스터에그 퀘스트**로 이관

## 미확정 결정 (구현 전 사용자 확인 필요)

1. 이스터에그 퀘스트의 구체 형태
2. 카드별 강화 상세 수치 표 (프로토타입은 임시 일괄 규칙 사용 중)
3. **4막 이후** 구상 (현재 3막 클리어 = 여정 완주). 3막 전용 별자리·카드 보강도 미정
4. 정식 환율표 개정 — 문서 표(뽑기 1장 = 물질 1)가 실제 빌드 카드와 불일치 (밸런스로그 12차)
5. ~~시뮬레이터의 맵이 아직 9행~~ → **해결 (2026-08-11)**: `sim_battle_v2.py`의 맵 모델을 빌드와 같은 13행(대장간·3막·휴식 30·막 사이 22 포함)으로 정렬 — 밸런스로그 18차
6. 필요 수가 2성에 몰렸던 시기의 메모 — v0.20에서 3성 9개가 부활했으므로 **별자리 개성 차등**(카드 수 vs 보너스 폭) 논의만 남음
